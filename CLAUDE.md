# CLAUDE.md

このファイルは**セッションの役割と共通の約束**だけを定める。
プロジェクトの前提（構成・技術スタック・ビルドとテストの回し方・コーディング規約・
デザインシステム・Git運用）は `AGENTS.md` に置いてある。着手前にそちらも読むこと。
同じ内容を両方に書かないこと——片方だけ古くなる。

## 共通ルール（セッションの役割によらず適用）

- 応答はすべて日本語で行うこと。
- 設計文書は `docs/` 配下を正とする。実装と設計が食い違った場合は、
  実装を優先せず設計文書を更新してから進む。
- 着手前に `docs/agent-implementation-guide.md` を読むこと
  （(仮)の解釈・スコープ規律・テスト・人間タスクの扱いを定めている）。
- 指示されたスコープ外の変更を先取りしない。

## セッションの役割について

このファイルは役割を規定しない。実装担当かレビュー担当か、
実行系コマンド（`xcodebuild` 等）の可否、コミット・pushの可否は、
**セッション開始時のプロンプトで指定する**。

指定がない場合は読解のみを行い、書き込みの前に確認を取ること。

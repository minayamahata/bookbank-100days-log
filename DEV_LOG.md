# BookBank 開発ログ

最終更新: 2026年08月21日

---

## プロジェクト概要

**BookBank（読書銀行）**
読んだ本の金額を貯金のように積み立てていく、知的エンタメiOSアプリ。
App Store公開済み・5言語対応（日本語・英語・韓国語・簡体字・繁体字）。

プロジェクト構成・技術スタック・ビルドとテストの回し方・コードの約束・Git運用は
**`AGENTS.md` を正とする**（ここには写さない——両方に書くと片方だけ古くなる）。
リリース計画の正は `docs/implementation-roadmap.md`。本書が持つのは
**現在のフェーズ・実装済み・未実装**の3つと、日付ごとの開発履歴。

---

## 現在のフェーズ（2026-08-17時点）

**R4.7（v1.7.0）— 再読履歴。実装完了（2026-08-18）。** 設計書 `docs/reread-history-spec.md`。実機での旧スキーマ→新スキーマ移行確認、5言語訳レビュー、受け入れは ✅ 完了（2026-08-19 オーナー確認）。**同日: データ安全性**（R4.7専用の移行前バックアップ・日付整合・編集の一括保存）。**2026-08-20: v1.7.0 build 9 のリリース前UIとして「新機能のお知らせ」を実装**（設計書 `docs/whats-new-message-design.md`）。**同日: TestFlight build 9 のオーナー確認を経て App Store 審査へ提出**（Dynamic Type・VoiceOverのみ未確認で残す）。次の大型リリースは R6。

**R4.5（v1.6.x）— メモつながり `[[ ]]` T1。実装・検収とも完了（2026-08-13）。**

- 直近の完了リリースは **v1.6.0（R4・リポジトリ抽象化・2026-08-07）**
- R4.5の実装ステップ5'〜9'（パーサ・表示・編集ツールバー・チップ行と絞り込み・検収）は完了（2026-08-11〜13）
- N0エクスポートによる既存メモとの衝突確認も完了——書籍421冊・メモあり16冊で検出52件、すべて意図した表示・誤装飾ゼロ（担保範囲込みで `docs/memo-tagging-design.md` 7.1節）
- 同乗予定だった「UI改善」の枠は**メモ編集ツールバーの新設に充当**した（`#` を打たせる入力負担の解消が必要になったため。バックログ自体は以降のリリースで改めて選定）
- **リリース前の人間タスクはクローズ（2026-08-15 オーナー確認）**: R4.5のUI文言5言語レビュー、書籍詳細シートの実機確認と `book.delete.action` の訳、通帳／本棚検索の実機確認、メモ一覧・本文検索・タブバー背景の実機確認はいずれもOK
- **R4.6（v1.6.x UIパッチ枠）を新設（2026-08-13）**——R6までの合間に発生する割り込みUI改善の置き場（直列計画の外の随時枠。入場条件などはロードマップ R4.6）。1件目の**書籍詳細のヘッダー吸い付き型シートは実装済み**。**2026-08-15: 個別に作った引用が保存後に1枠へ合体する問題を修正**。**2026-08-15: メモ一覧ページ（メモのノートページ）を実装**。**2026-08-16: スクリーンショットの画像候補（マンスリーログ共有）を実装**——カレンダー表示中のスクリーンショット、または月ヘッダーの共有ボタンから透明PNG 4種をコピー／保存／共有できる。同日の完了前補正で、カレンダーが実際に前面のときだけ自動表示し、表紙準備はキャッシュ優先＋同一URLの最大4件並列、共有セルは2:3にした（設計メモ `docs/monthly-log-share-design.md`）。**同日: アプリ内タイポグラフィを LINE Seed へ全面移行**（設計メモ `docs/typography-design.md`）。**同日: マンスリーログ共有の外部PNGを描画範囲＋24pxへクロップ**（内部マスターは1080×1920のまま）。**同日: 日本語フォントを Google Fonts 公式の 2025年版へ交換**——2024年 App 用 OTF は文字本体（cap height）は同じだが上下メトリクスが大きく、`leading(.tight)` では ascender／descender を変えられず実機で変化がなかった。tight leading は撤回し、文字サイズや画面 padding は変更していない。**同日: 日本語の通常 SwiftUI 画面だけ行間 1pt**——差し替え後の複数行が詰まりすぎて見えるため。共有 PNG・他言語・UIKit は 0 のまま。**2026-08-20〜21: マンスリーログ共有へ背景モード3種（白・黒・透明）を追加**——白は黒文字系パレットの不透明JPEG、黒は不透明JPEG、透明PNGは現行のまま。初期選択は白（設計メモ `docs/monthly-log-share-design.md`「背景モード」）。写真を選んでログを重ねるモードも試作したが、読書管理アプリとしての範囲と調整・保守コストが見合わないため、2026-08-21のオーナー判断で同日撤去した。R4.6の登録項目は一通り実装済み。**ウィジェットはこの枠に入れず R6完了後**（App Groupのデータ受け渡しがFirestore移行で作り直しになるため）
- **R4.7（v1.7.0 再読履歴）を R6 の前に独立実施（2026-08-17）**——1冊の本に初回日＋複数の再読日を持たせ、通帳・カレンダー・統計・口座集計・口座Markdown・マンスリーログ共有を回数ベースにする。本棚は1書影のまま。スキーマ変更があるため R4.6 には入れない
- 次の大型リリースは **R6（v2.0.0）クラウド移行**（Firebase Auth + Firestore）。以降は R8 Web v1 → R9 課金刷新 → R5 ノードグラフ → … と**直列**で進める（ロードマップ 1章）

---

## データモデル（SwiftData）

R4（v1.6.0）以降、**View層はモデルを直接触らない**——取得・保存はリポジトリ
（`Repositories/`・DTO）経由で、`@Model` を扱うのはリポジトリ実装と移行コードのみ。
全モデルにR3で `uuid`（R6のFirestore docID用の安定ID）が入っている。
R6でこの4モデルがFirestoreへ移行される（`docs/cloud-migration-architecture.md` 3.3節）。

### 1. Passbook（口座・通帳）

書籍を分類・管理する論理的なグループ。
`name` / `type`（overall/custom）/ `sortOrder` / `isActive` /
`colorIndex`・`customColorHex`（テーマカラー。カスタム色はUnlimited機能）/ `createdAt` / `updatedAt`。
`userBooks` へカスケード削除のリレーション（口座を消すと本も消える）。

### 2. UserBook（ユーザー登録書籍）

書籍マスター情報とユーザー固有情報を統合したモデル。

- **マスター情報**: `title` / `author` / `isbn` / `publisher` / `publishedYear` / `seriesName` /
  `price` / `imageURL` / `coverImageData`（手動登録の表紙・外部ストレージ）/
  `bookFormat` / `pageCount` / `source`（api/manual）
- **ユーザー固有**: `memo`（プレーン文字列。R4.5の `[[ ]]` つながりや書式記号もこの中の文字にすぎない）/
  `isFavorite` / `priceAtRegistration`・`currencyCode`（資産計算用）/ `registeredAt` /
  `rereads`（R4.7。`[RereadRecord]?`。nil=履歴なし。再読1件は id と date のみ）
- **リレーション**: `passbook`（所属口座）/ `readingLists`（逆参照）

### 3. ReadingList（読了リスト）

ユーザーが作るキュレーションリスト。`title` / `listDescription` / `colorIndex` /
`bookIds`（並び順。R3で旧 `bookOrderData` からUUID配列へ移行）/ `books` リレーション。

### 4. MonthlyMemo（月別メモ）

口座を横断して年月ごとに書くメモ。`year` / `month` / `text`。

### （レガシー）Subscription

スキーマには残っているが、**課金判定には使っていない**——現在の課金は
`Services/UnlimitedManager.swift`（StoreKit 2・`Transaction.currentEntitlements`）で判定する。

---

## 実装済み機能（リリース履歴ベース）

画面ごとの細かい変更は下の「開発履歴」に日付つきで残っている。ここでは
「何ができるアプリか」と「どのリリースで入ったか」を持つ。

### 基盤（〜v1.3系・2026年1〜3月の集中開発）

- **口座（通帳）**: 複数口座＋総合口座（全口座の合算ビュー）。口座ごとのテーマカラー
  （カスタム色はUnlimited）・追加/編集/削除・初回起動のオンボーディング
- **本の登録**: 書籍検索（楽天ブックス・Google Books・NAVER。切り替え可能）・
  バーコードスキャン・手動登録（表紙はカメラ/ライブラリから）
- **通帳画面**: 登録した本が入金のように記帳される
- **本棚**: 4カラムグリッド・月別カレンダービュー・フィルター（お気に入り/メモ）
- **本の詳細・編集**: 表紙・価格・詳細情報・メモ（全画面エディタ）
- **月別メモ**: 年月ごとのふりかえりメモ（カレンダービューから編集）
- **読了リスト**: 本を選んでリスト化・並べ替え・Web共有（別リポジトリ `bookbank-share` がURLを発行）・
  Markdownダウンロード
- **統計**: 年別の金額・冊数グラフ（Swift Charts）・年別サマリー
- **Unlimited課金**: StoreKit 2（年額/買い切り）。口座・読了リスト無制限、詳細エクスポートなど
- **5言語ローカライズ**（ja/en/ko/zh-Hans/zh-Hant）・ダークモード対応

### R1: v1.3.1 — 残バグ即応 ✅（2026-07-07〜09）

価格・通貨記号・NAVERの未販売本の表示などのバグ修正（内訳はロードマップのR1表）。

### R2: v1.4.0 — 検索改修＋本棚内検索 ✅（2026-07-09・App Store公開）

- 書籍検索の非同期基盤を整理（検索の世代管理・エラー時の再試行UI・3プロバイダのページング統一）
- **本棚内検索を新設**: フィルター行が検索フィールドに変形し、所有本をタイトル・著者で
  ライブ絞り込み（`ShelfSearchMatcher`。仕様は `docs/bookshelf-search-spec.md`）

### R3: v1.5.0 — UUID導入 ✅（2026-07-09）

クラウド移行の土台。全4モデルに `uuid` を追加（移行前バックアップ→バックフィル→整合性検証の
防御層つき）。読了リストの並び順をUUID配列へ移行（設計メモ `docs/r3-uuid-migration-notes.md`）。

### N0スパイク ✅ クローズ（2026-07-11〜19・リリースなし）

ノードグラフ（R5）の方式検証。実本棚約400冊＋5言語テストデータで評価し、合否基準をクリア・
パラメータ確定。成果物（評価ハーネス `tools/n0-spike/`・辞書・`docs/n0-spike-results.md`）は
R5着手まで凍結。

### R4: v1.6.0 — リポジトリ抽象化 ✅（2026-08-07）

全Viewの `@Query` をリポジトリプロトコル（`Repositories/`・DTO）経由に置き換え、
**View層からの `@Model` 参照をゼロに**。見た目・挙動を変えない内部改修で、R6で背後を
Firestore実装に差し替えるための土台（設計メモ `docs/r4-repository-abstraction-notes.md`）。

### R4.5: v1.6.x — メモつながり T1 ✅ 完了（2026-08-07〜08-13）

- **共有IDのuuid化** ✅（2026-08-07）: 再共有のたびに別URLが発行される不具合の解消
- **メモつながり `[[ ]]`** ✅ 実装完了（2026-08-11〜12）: メモに `[[ラベル]]` と書くと、
  同じラベルを書いた本同士がつながる。スキーマ変更ゼロ（メモ文字列のみ）。
  メモ編集ツールバー（つなぐ・太字・引用・ページ番号・書式クリア・ひとつ戻す）、
  編集画面のライブ装飾（記号を隠す）、詳細画面のつながりチップ行、本棚の絞り込み、
  検索モードのつながり一覧（設計メモ `docs/memo-tagging-design.md`）
- **検収（ステップ9'）** ✅（2026-08-13）: ドキュメント同期＋N0エクスポートの衝突確認（誤装飾ゼロ・設計メモ 7.1節）
- リリース前の人間タスクだった**UI文言の5言語レビュー**は ✅ 完了（2026-08-15 オーナー確認）

---

## 未実装機能（リリース割り当て済み）

正は `docs/implementation-roadmap.md`（依存関係・完了条件つき）。以下は実施順の要約で、
食い違いが出たらロードマップを優先し本表を直す。

| 順 | リリース | 内容 |
|----|---------|------|
| 1 | **R6 v2.0.0 クラウド移行** | Firebase Auth（ログイン）・Firestore・移行ウィザード・アカウント削除・エンタイトルメント同期（WebからUnlimitedを判定するため）・解析基盤・M2（ノード増枠）スキーマ |
| 2 | **R8 Web v1** | iOS同等フル機能のWeb版（日本語UIのみ・文言は初日からi18n外部化。v1.1で韓国語版） |
| 3 | **R9 v2.1.0 課金刷新** | RevenueCat化・月額プラン新設・買い切り販売停止（既存購入者の権利は引き継ぐ） |
| 4 | **R5 ノードβ→本リリース** | ノードグラフ（本と本のつながりの可視化。N0で方式検証済み）・M1冊数枠（無料10冊）・T2つながりエッジ・**つながりラベルの一覧＋リネーム/統合**（R4.5からの申し送り・必須要件） |
| 5 | **AF アフィリエイト** | publicShelves（β→本リリース） |
| 6 | **R7 発見D1 → R10 D2+M3** | 匿名集計の裏側（D1）→ 点線レコメンド＋チケット（D2+M3。D1から1〜2ヶ月の蓄積後） |
| 7 | **R11 Web課金** | RevenueCat Web Billing・特定商取引法に基づく表記 |
| - | **EM1/EM2 読了リスト公開・埋め込み** | R6完了後ならいつでも可。時期はWeb v1公開後の隙間で判断 |
| - | ~~**残バグ（低優先）**~~ | A-9（形態フィルター中の補完取りこぼし）・B-5（Google価格の丸め誤差）は **2026-08-13 にクローズ**（直さない判断。5リリース分の見送りにより実害なしと判断）。症状は `docs/bug-review-2026-07-06.md` に残してあるので、同じ症状に気づいたら既知として扱う |

---

## 開発履歴

### 2026-08-21
- ✨ **マンスリーログ共有へ4枚目テンプレート「丸印カレンダー」を追加し全6枚に（R4.6）**
  3枚目と同じ4:5（360×450／1080×1350）の新テンプレート `circledCalendar`。書影を使わず、左上に英語月名＋年、右上に冊数、中央に数字だけのカレンダー（読書日は丸い輪郭＋白の日付、それ以外は不透明度0.38）、下にワードマーク。金額なし。カルーセル順は 1 calendarSummary・2 largeMonth・3 verticalMonth・4 circledCalendar・5 minimalSummary・6 monthInBooks。ページドット・出力キャッシュ・3背景モードは自動追従。テスト期待値（4:5・金額非依存・読書日で変化・見出し）を追加し、ユニットテスト全件パス・exit 0。追調整で冊数表示を削除し、月名＋年を中央揃えへ（見た目のみ・Debugビルド確認）。
- ✨ **マンスリーログ共有へ5枚目テンプレート「My month in books」を追加（R4.6）**
  1:1（360×360／1080×1080）の新テンプレート `monthInBooks`。左揃えで英語固定の月名＋年（12pt）、タイトル「My month in books」（32pt・2行）、冊数（24pt＋単位11pt上付き・単位はアプリ言語）、BookBankワードマークを縦中央に置く。金額・カレンダーは載せず、背景3モード（白・黒・透明）とページドットはテンプレート数に自動追従。テンプレート・レイアウト定数・描画・テスト期待値（金額非依存・冊数で変化・見出し・比率）を追加し、ユニットテスト全件パス・exit 0。追調整でタイトルの行間を負パディング8ptで詰め、単位「冊」の太字をやめ、全5テンプレートのワードマーク高さを12ptへ統一・縮小（旧16〜18pt。見た目のみ・Debugビルド確認）。
- 🗑 **写真合成モードを撤去し、背景を白・黒・透明の3モードへ確定（オーナー判断）**
  2026-08-20〜21に、写真背景（`PhotosPicker`選択・正規化・テンプレート比率への中央クロップ）、配置編集画面（ドラッグ・ピンチ・白黒文字・Instagram風の中央吸着・ガイド・触覚・ヒット領域とSafe Areaの補正）、合成JPEG出力までを試作したが、読書管理アプリとしての範囲と調整・保守コストが見合わないため、2026-08-21のオーナー判断で同日撤去した。専用のUtils 4本と編集View・テスト2本・5言語文言7キー・設計記述を削除し、白・黒・透明に必要な共通処理（`MonthlyLogSharePalette`・`MonthlyLogShareExportAsset`・PNG/JPEG共通の保存/コピー/共有・`MonthlyLogShareImageTrimmer`）は残置。初期選択は白のまま。本の表紙選択（AddBook/EditBookのPhotosPicker）は無関係のため不変。

- 🔁 **共有画像の比率を 9:16／4:5／1:1 へ差し戻し（オーナー判断）**
  下記の 3:4／8:9／1:1 化を実機確認の結果、同日中に元の構成へ戻した。1・2枚目 9:16（360×640／1080×1920・透明PNGはアルファ境界＋24pxトリム）、3枚目 4:5（360×450／1080×1350）、4枚目 1:1（360×360／1080×1080）。`MonthlyLogShareCanvasFormat` の名称・トリミング経路・テスト・設計文書も含めて差し戻し、プレビュー表示比率90%だけ維持。スキーム全件（UIテスト含む）410件パス・失敗0・スキップ0・exit 0。
- ✨ **（同日中に差し戻し）マンスリーログ共有画像の比率を 3:4／8:9／1:1 へ変更し、透明PNGのトリミングを廃止（R4.6）**
  プレビューと全保存形式を、1・2枚目 3:4（360×480／1080×1440）、3枚目 8:9（360×405／1080×1215）、4枚目 1:1（360×360／1080×1080）へ統一した。`MonthlyLogShareCanvasFormat` を `threeFour` / `eightNine` / `square` へ改名し、白・黒・透明PNG・写真合成JPEG（中央クロップ・配置編集含む）のすべてが新比率を使う。
  - 透明PNGのアルファ境界＋24pxトリミングを廃止し、全テンプレートともキャンバス全体を出力してプレビューと保存画像を完全一致させた。`MonthlyLogShareImageTrimmer` は写真モードのログ実体トリム用として残置（`trimmedLogContent` が使用中。24px余白の出力経路だけが廃止）
  - プレビューは3:4カードを基準幅にし、3・4枚目は3:4基準領域内で縦中央のまま。カード間隔・見切れ・ドット・スウォッチ・アクション行の位置は不変
  - 1〜3枚目のカレンダーは既存の動的セル縮小で4〜6行を新キャンバスへ収め、月名・金額・曜日・ロゴは省略しない
  - 追調整でプレビューカードの表示比率（`displayScale`）を85%→90%へ拡大（見た目のみ・Debugビルド確認）
  - テストは論理サイズ・保存サイズの固定を新値へ更新し、トリミング前提のヘルパーを全テンプレート共通の「キャンバス全体維持」検証へ統合。クロッパー・プレビュー寸法・背景レンダラの期待値も再計算。スキーム全件（UIテスト含む）410件パス・失敗0・スキップ0・exit 0
- 🎨 **マンスリーログ共有画面の縦方向余白を確定（オーナー指示）**
  見た目上の距離で、ヘッダー領域下端→プレビューカード上端を最小24pt、9:16カード下端→ページドット上端を6pt、ページドット下端→背景スウォッチの見える外周（38ptリング）上端を10ptにした。ヘッダー側の下余白8ptを廃止してカルーセル側の24ptだけで管理し、GeometryReaderの未使用領域ではなく実際の間隔として確保（カード高さの計算からも24ptと固定行分を控除）。スウォッチはタップ領域44ptと見える外周38ptの差の片側3ptを差し引き、レイアウト間隔7ptで見た目10ptを実現。間隔はViewのprivate定数（`headerToPreviewMinimumSpacing`等）へ一元化。3・4枚目の縦中央配置は維持し、短いカードでは見た目の余白が6ptより広がることを許容。見た目のみの変更のためDebugビルド確認（exit 0）・テストは省略。設計メモの余白値も更新。

- 🎨 **マンスリーログ共有の見た目調整（オーナー指示）**
  背景スウォッチを一回り縮小（丸34→30pt・タップ領域44→40pt）し、間隔を18→10ptへ。境界線は非選択にも2ptを付け、選択中は白・非選択は白28%で示す。2枚目の月略称（Aug）と4枚目の2桁月（08）を60→52ptへ縮小。カルーセルとページドットの間を詰め（配置ボタン行の上余白6ptとドット上余白8ptを撤去）、スウォッチ下の余白を18→30ptへ拡大。追調整で、二重線に見えた境界線を丸の縁へ直接描く形（`strokeBorder`・内側余白なし・黒丸の個別縁取り廃止）へ、4枚目の月を40ptへ、**初期選択の背景を透明から白へ変更**（`defaultMode = .white`・対応テストも更新）。数値は `MonthlyLogShareTemplateLayout`（Utils）を含むためユニットテスト全件396件パス・exit 0。さらに見た目補正として、ページドットをカルーセル直後（9:16カード下8pt）へ移し「配置を調整」をドット下へ（3・4枚目の縦中央配置と横スワイプは不変）。スウォッチは中身30pt／不透明グレー外枠32pt・1pt／白い選択リング38pt・2pt／タップ44ptの3層構造へ分離し、市松の透けをなくして白背景の選択状態も識別できるようにした（見た目のみのためDebugビルド確認・exit 0）。あわせて、タイトルとカルーセルの間に溜まっていた余白を解消——カード下寄せをやめてカルーセルをタイトル直下から始め、カード高さを「利用可能高−固定行分」で算出し、余りはスウォッチとアクション行の間のSpacerへ移した（コピー／保存／その他の位置・カード寸法計算・横スワイプは不変）。共有画像本体も調整——3枚目はカレンダー列を下端へ寄せてロゴとの距離を1・2枚目と同じ16ptに、4枚目は金額と冊数の間隔を20→36ptへ、1・4枚目の「合計金額」「冊数」ラベルを削除して数値＋単位のみに（描画変更のためユニットテスト全件396件パス・exit 0）。同日追調整で、1・4枚目の通貨記号（¥）と単位（冊）を20→14ptへ縮小、2・3枚目のBookBankロゴを右揃えへ、4枚目の2桁月を36ptへ、4枚目の金額と冊数の間隔を48ptへ。さらに1〜3枚目を「下端寄せ＋ロゴ固定」からロゴ込みのコンテンツ一式をカード縦中央へ置く構造へ変更（カレンダー→ロゴ16ptは維持。ユニットテスト全件371件パス・exit 0）。続く微調整で、2枚目ロゴを中央揃えへ戻し、3枚目ロゴはカレンダー右端に揃え、4枚目の月と年の間隔を2pt拡大、カード下端→ページドットを6→10ptへ（見た目のみ・Debugビルド確認）。設計メモの更新日と背景モード節へ反映済み。

### 2026-08-20
- ✨ **マンスリーログ共有へ背景モードを追加（R4.6）**
  共有画面のページドット下に背景スウォッチを置き、テンプレートのスワイプと独立に背景を切替可能にした。白背景は見出し・曜日・空セル・ワードマークを黒系へ反転した不透明JPEG（表紙上の日付と`+N`は白維持）、黒背景は不透明JPEG、透明PNGは現行仕様のまま。出力は`MonthlyLogShareExportAsset`へ統一し、コピー／保存／共有のUTTypeと拡張子をPNG/JPEGで切替、ページ・背景をキーに遅延生成してキャッシュ。5言語の文言を追加。写真を背景へ敷くモードも同時に試作したが、翌21日のオーナー判断で撤去した（同日の記録参照）。

- 🚀 **v1.7.0 build 9 を App Store 審査へ提出（オーナー操作）**
  TestFlight build 9 の実機確認をオーナーが完了（自動表示の規則・スワイプ／×／はじめる・設定からの再表示・5言語表示とイラスト目視・ライト／ダーク・翻訳レビュー・データ保持。**Dynamic Type と VoiceOver のみ未確認で残す**——公開はブロックしない）。リリースノートとプロモーション用テキストは5言語で登録。公開操作はApp Store Connect待ち。
- 🐛 **メモ一覧が、再読しても最新の読書日順に並ばなかった（R4.7の追随漏れ）**
  一覧はリポジトリの正準順（初回登録日降順）のままだったため、昔登録した本を最近再読してメモを書いても上部へ来なかった。本棚と同じ `ReadingTally.sortedByLatestReadDate`（`latestReadDate`＝初回登録日と再読日の最大・降順・同値は正準順維持）を、メモの絞り込みの前に全体へ当てるようにした。検索はfilterのみで再ソートしない。メモ編集日時順ではなく、`updatedAt`・正準ソート・通帳などの初回登録日順画面は変更なし。保存済みの `rereads` から計算するためデータ移行も不要（設計メモ `docs/memo-list-design.md`「並び順」）。
- 🐛 **メモ編集で、引用の下からのデリート長押しがページ番号でテンキーに変わり「3」が入っていた**
  カーソルがページ番号行へ来た理由を区別せずテンキーへ切り替えていたのが原因。下からの後退削除は削除要求（`shouldChangeTextIn`）の時点で出典ページ行をひとまとまりとして飛び越え、引用本文側の削除へ振り替えるようにした（`p.` も `p.42` も対象。テンキーは一瞬も出ず、キーリピートごとに削除が前進する）。
  - 通過中はページ番号を保持し、途中でやめても引用とページ番号は壊れない。そのまま削除を続けて引用自体が消えるときだけ、付随するページ行を改行ごと一緒に消す（ページ番号だけが本文に残らない）
  - 判定は純粋関数 `MemoQuotePage.backwardDeletionRedirect` / `skipDeletion` へ分離し、飛び越え中かどうかはCoordinator内の編集セッション状態（期待キャレット位置）だけで持つ。書き換えは従来どおりテキストビュー経由で「ひとつ戻す」に載る
  - タップでのテンキー編集・引用内Enterでのページ番号移動・空の `p.` の削除保護・単独の `p.42`・範囲選択削除・ツールバーの引用解除は従来どおり（設計メモ `docs/memo-tagging-design.md` 更新日 2026-08-20）
- 🐛 **メモ編集で、変換中に「ページ番号を入力」「つながりを入力」の案内が文字や囲みに重なっていた**
  案内の描画位置は最後の全体装飾時の文字範囲のままで、IME変換中は当て直さないため、変換で本文が伸びるとズレて重なった。変換中は案内を描かないようにし（確定後に正しい位置へ戻る）、引用の囲みの範囲だけは変換中も本文から取り直して追従させた。
- 🐛 **メモ編集で、引用の中の入力中の文字が本文サイズで出ていた**
  変換を確定した瞬間に縮んで見えた。IME変換中は装飾を当て直さない防御があるため、つながりと同じく**打つ前に**引用のフォントサイズと文字色を `typingAttributes` で決めるようにした。判定はキャレット行の `> ` 接頭辞。
- 🎨 **メモ編集の破棄確認の取り消し側を「編集を続ける」にし、テーマ色を外した**
  新キー `memo.discard.continue_editing`（5言語）。画面へ `.tint(.primary)` を付ける口座編集・書籍編集と同じ方式で、破棄は赤のまま。ツールバーの保存は `accentColor` を明示して従来どおり。
- ✨ **新機能のお知らせを実装（v1.7.0 build 9 のリリース前UI）**
  v1.7.0の4機能（再読の記録・メモの文字装飾・本をつなぐ・マンスリーログの共有）を、背景を暗くした中央カード4ページで知らせる。左右スワイプと「次へ」で移動、「×」は一時的に閉じ、「はじめる」で確認済み。設計書 `docs/whats-new-message-design.md`。
  - 自動表示はお知らせバージョン `1.7.0` につき最大3回・1起動につき1回。既存ユーザーはスプラッシュ後、新規ユーザーはオンボーディング完了後。3回表示後や確認済み後は自動表示しない
  - 設定のサービス案内へ「新機能」行を追加（フォントライセンスの上）。手動表示は回数へ加算せず、3回終了後もいつでも再表示できる
  - 表示方針は純粋型 `WhatsNewAutoShowPolicy` + `WhatsNewStore`（UserDefaults・バージョン単位キー）で分離し、ユニットテストを追加
  - 画像 `img_news-01〜04`（900×700・RGBA。同日オーナーが1000×700から差し替え）を `.imageset` 化。PNGのピクセルデータは無変更
  - 同日オーナーフィードバックで体裁を調整——カード高さは内容準拠、ドットは画像の直下、タイトル上下の余白を拡大、日本語説明は読点位置で固定改行（読点削除・句点は残す）、画像はカード横幅100%、ボタンは総合口座の口座カプセルと同じ `passbookCapsuleGradient`
  - 同日フィードバック第2回——ライトモードのカード背景を `#ECECEC`（View限定色）、説明は行間+4で文字色 `.primary`、カード上辺32pt・説明と画像の間24pt。**実機チェック用に `temporarilyAlwaysAutoShow = true` の一時スイッチを追加**（確認済み・3回上限を無視して毎起動表示。回数は加算しない）。**同日、実機チェック完了により false へ復帰済み**（リリース版は必ず false のまま）
  - 同日フィードバック第3回——カード内の縦スクロールを廃止（`ScrollView` を外す。高さ上限超過時は下端で切れるためDynamic Type最大は実機確認項目）。スワイプ中に見出しが「×」の上へ重なるため、閉じるボタンをカードの `ZStack` 最上層（`zIndex`）へ移動
  - 同日フィードバック第4回——見出し（新機能 01）を footnote→subheadline Bold へ拡大、操作ボタン上の余白を20→32pt（カードはその分高く）、02の説明を「ページ番号・引用・太字を使って…」の列挙順へ差し替え（5言語とも同順）
  - 同日、**5ページ構成へ変更**——03の説明を「本と本がつながりはじめます。」へ、旧04（マンスリーログの共有）を05へ移し、新04「メモ一覧ページ」（本に残したメモだけを集めて検索）を追加。画像はページ番号に追従（03・04はオーナー差し替え、05は新規900×700をimageset化）。5言語同時登録・テストも5件構成へ更新
  - 新規文字列は5言語同時登録（つなぐ・マンスリーログ等は既存キーの表現を再利用）。build番号を 8→9 へ
  - **人間タスク**: build 8→9 上書きでのデータ保持、自動表示3回・×・はじめる・設定からの再表示の実機確認、5言語表示とPNG内日本語イラストの目視判断、ライト／ダーク・Dynamic Type・VoiceOver、5言語のネイティブ翻訳レビュー

### 2026-08-19
- 🎨 **本の編集で、登録口座を書影の下へ移した**
  フォーム最上部にあった項目を、表紙の直後・登録日の前にした。
- 🎨 **集計の年別ページが、日本語以外でカード見出しを切っていたのを直した**
  原因は年表示ではなく、628pt 固定の `TabView` が中央配置のまま高い `YearlyChartContent` をクリップしていたこと。年は従来どおり 34pt Bold・上8pt・下16pt。ページを上揃えし、高さを実測値へ追従させた。
- 🔧 **R4.7 のデータ安全性を補強**
  再読スキーマ専用の移行前バックアップ（`didValidateRereadSchemaV1` / `PreRereadSchemaMigrationBackupV1`）を R3 と分離して追加。生成失敗時は R4.7 を優先し、無い／復元できない場合は R3 バックアップへ戻す。登録日と再読日の整合を `RereadDatePolicy` で保証し、編集画面の保存を1回の `saveBookEdits` にまとめた。
- ✅ **R4.7 の実機更新確認をクローズ（オーナー確認）**
  既存データを残したまま新版を上書き起動し、問題なし。旧スキーマ→新スキーマの移行確認は完了。
- ✅ **R4.7 の人間タスクをクローズ（オーナー確認）**
  5言語訳レビューと受け入れは問題なし。残件なし。

### 2026-08-18
- 🎨 **通帳の再読行に、テーマ色の丸と繰り返しアイコンを足した**
  「再読」の左。色の見え方は通帳の＋ボタンと同じ。
- 🎨 **本の詳細だけタブバー下が透けなかったのを、通帳と同じセーフエリア方式へ直した**
  シートを物理下端までレイアウトし、`bottomCoverage` の手動延長をやめた。タブバー背景の hidden 指定は使わない。
- 🎨 **本の編集の削除確認で、キャンセルをテーマ色から戻した**
  口座編集と同じく画面に `.tint(.primary)` を付け、タブのテーマ色がアラートに乗らないようにした。
- 🎨 **マンスリーログ共有のドット下余白を広げた**
  ページドットとコピー／保存／その他のあいだを 16pt から 32pt にした。
- 🎨 **本の詳細のタブ下を他ページと同じ透け方に戻した**
  `.toolbarBackground(.hidden, for: .tabBar)` を外し、タブメニュー下の背景色塗りをやめた。
- 🎨 **確定メモの引用に先頭行の横線を追加**
  枠の左へ少しはみ出す短い線。編集画面には出さない。
- 🎨 **本棚の再読マークを削除**
  書影右上の `icn_repeat` は出さない。メモ・お気に入りバッジはそのまま。
- 🎨 **マンスリーログ共有から「おすすめ先」「Instagram Stories」を削除**
  コピー・保存・その他のボタンはそのまま。
- 🎨 **メモ編集の装飾ボタンの並びを差し替え**
  左から つなぐ・ページ番号・引用・太字・書式クリア・ひとつ戻す。
- 🎨 **メモ編集の装飾ボタンから区切り線を外し、均等間隔にした**
  「つなぐ」の右と「ページ番号」の右の縦線を削除。ボタン幅を等分してアイコン間隔を揃える。
- 🎨 **引用ボックスの下辺だけ 2pt 狭くした**
  内側余白は上左右 18、下 16。編集画面と確定メモで揃える。
- 🎨 **メモ編集の装飾ボタンのラベルを小さくした**
  「つなぐ」「太字」などの文字を 10pt から 8pt にした。アイコンサイズは据え置き。
- 🎨 **メモ編集と確定メモの行間を広げた**
  折り返しだけ +6。メモ一覧のプレビューは据え置き。
- 🎨 **編集画面の「登録口座」を最上部へ**
  表紙・登録日より上に出した。
- 🎨 **本棚の再読マークを `icn_repeat` に差し替え、回数を削除**
  書影右上はアイコンだけ。`×N` は出さない。
- 🎨 **編集画面から「読書履歴」見出しを削除**
  再読の日付変更・削除の行はそのまま。見出しだけ外した。
- 🎨 **詳細の読書履歴マーカーを透過なしに**
  ●と縦棒は `Color.primary` のまま、不透明度は100%にした。
- 🐛 **同日の再読を1件消すと全部消えていた**
  同じ日に再読が複数あっても、消すのはその1件だけ。登録日と、同日の残りの再読は残す。
  - リポジトリは一致する ID の先頭1件だけ消す。追加時に ID が重なったら採番し直す
  - 編集画面は消す行の ID を明示して保存する。日付や配列の差分では消さない
- ✅ **R4.7 / v1.7.0 再読履歴を実装完了**
  1冊の本に初回読了日と複数の再読日を持たせる。本棚は1書影のまま、通帳・カレンダー・統計・口座集計・口座Markdown・マンスリーログ共有は読書回数分を加算する。
  - 設計書 `docs/reread-history-spec.md`。旧スキーマfixtureを新スキーマで開き、既存データ保持と `rereads == nil` を確認
  - `BookBankTests` は iPhone 17 / iOS 26.5 で exit 0
  - `updatedAt` は再読の追加・編集・削除では動かさない（R6 の LWW 見直しへ申し送り）
  - **人間タスク**: 新規文字列5言語の訳レビュー、受け入れ、実機移行確認は ✅ 完了（2026-08-19 オーナー確認）

### 2026-08-17
- 🔧 **R4.7 / v1.7.0 再読履歴を着手**
  1冊の本に初回読了日と複数の再読日を持たせる。本棚は1書影のまま、通帳・カレンダー・統計・口座集計・口座Markdown・マンスリーログ共有は読書回数分を加算する。
  - 設計書 `docs/reread-history-spec.md`。ロードマップに R4.7 を新設
  - `updatedAt` は再読の追加・編集・削除では動かさない（R6 の LWW 見直しへ申し送り）
  - **人間タスク**: 新規文字列5言語の訳レビュー、TestFlight / 実機での旧スキーマ→新スキーマ移行確認
- ✅ **マンスリーログ共有の3枚目を正方形から4:5へ**
  カルーセル順は維持したまま、3枚目（verticalMonth）だけ 1:1 から 4:5 にした。1・2枚目は 9:16、4枚目は 1:1 のまま。形式はテンプレート自身が持ち、表示順の index では決めない。
  - 3枚目: 論理 360×450、マスター／出力 1080×1350。トリミングせずキャンバス全体を出力して 4:5 を維持
  - 追加の縦 90pt は主にカレンダーと上下余白へ使う。月名 34pt・年 12pt・セル 2:3・未登録日セル白 20% は維持
  - 1・2枚目: 論理 360×640、マスター 1080×1920。外部PNGは従来どおり alpha 境界＋24px へトリミング
  - 4枚目: 論理 360×360、マスター／出力 1080×1080。トリミングせず 1:1 を維持
  - プレビューは内部マスター全体を各テンプレートの比率（9:16 / 4:5 / 1:1）で表示する。外部PNGは流用しない
  - 設計メモ `docs/monthly-log-share-design.md`
  - **人間タスク**: 4〜6行月の3枚目、12か月の月名サイズ、February と August の月名、日曜始まり／月曜始まり、未登録日セル白20%、コピー／保存／共有が同じ画像、実機SNS編集（X・Instagram）での拡大・移動、写真アプリで3枚目が1080×1350・4枚目が1080×1080、3枚目に9:16や1:1用の市松余白が付かないこと
- ✅ **マンスリーログ共有の3・4枚目を正方形へ**
  カルーセル順は維持したまま、1・2枚目（calendarSummary / largeMonth）は縦長、3・4枚目（verticalMonth / minimalSummary）は正方形にした。形式はテンプレート自身が持ち、表示順の index では決めない。
  - 縦長: 論理 360×640、マスター 1080×1920。外部PNGは従来どおり alpha 境界＋24px へトリミング
  - 正方形: 論理 360×360、マスター 1080×1080。トリミングせずキャンバス全体を出力して 1:1 を維持
  - プレビューは内部マスター全体を各テンプレートの比率で表示する。外部PNGは流用しない
  - 3枚目は 360×360 のネイティブ配置（4〜6行月・長い英語月名・セル 2:3）。4枚目は月・年／金額・冊数／ロゴを正方形内で取り直した
  - 4・5行月だけ月名が 42pt になる問題を修正し、August 基準の月名 34pt・年 12pt へ統一した。カレンダーの行数別余白・セル調整は維持
  - 共有画像の未登録日セルを白 14% から白 20% へ変更。アプリ本体のカレンダーは変更なし
  - 設計メモ `docs/monthly-log-share-design.md`
  - **人間タスク**: 4〜6行月の3枚目、12か月の月名サイズ、5言語の金額・冊数、コピー／保存／共有が同じ画像、実機SNS編集（X・Instagram）での拡大・移動、写真アプリで3・4枚目が1080×1080、3・4枚目に9:16用の市松余白が付かないこと
- ✅ **同日複数冊一覧の行を通帳と同じ情報配置へ補正**
  本棚カレンダーで同じ日に複数冊あるときの一覧シートを、「表紙｜書名・著者｜右端に金額」にした。金額は既存の `BookPriceText` を再利用。日付はシートタイトルにあるため行内では出さない。
  - 表紙に対して、書名・著者と右端の金額を縦中央へ揃えた
  - 横方向の配置、金額表示、遷移は変更していない
  - データ契約・並び順・dismiss後に詳細を開く遷移は未変更
  - **人間タスク**: 5言語の書名・著者・金額、JPY以外の右揃え、長い書名、著者なし、金額なし、Dynamic Type拡大時の重なり、行タップ後のdismiss→詳細

### 2026-08-16
- ✅ **マンスリーログ共有のプレビューを内部マスター表示へ戻した**
  コピー／保存／共有用にトリミングした PNG をプレビューへ流用していたため、レンダリング完了後に余白と配置が変わって見えた。プレビューは常に 9:16 の `MonthlyLogShareCanvas` 全体。外部へ渡す PNG だけアルファ境界＋24px。
  - 設計メモ `docs/monthly-log-share-design.md`
- ✅ **日本語の通常 SwiftUI 画面だけ行間 1pt（Google Fonts 版置換後の微調整）**
  Google Fonts 版 LINE Seed JP の 17pt 行高は約 18.70pt（SF Pro は約 20.0pt）。大きすぎる行高は解消したが、複数行が少し詰まりすぎて見えた。
  - ルートで日本語だけ `lineSpacing(1)`。英語・韓国語・繁体字・簡体字は 0。`system` は実効言語へ解決
  - 1行テキスト・文字サイズ・画面 padding は変えない。Dynamic Type の倍率は行間へ重ねない
  - 共有キャンバスは `lineSpacing(0)` で固定。UIKit / MemoEditor は未変更
  - `leading(.tight)` / `leading(.loose)` は使わない
  - 設計メモ `docs/typography-design.md`
  - **人間タスク**: 日本語の複数行（書名・説明・オンボーディング・Paywall・詳細・設定）、約 1pt だけ広がっていること、1行リスト／ボタンの高さ、他言語不変、MemoEditor 不変、共有 PNG の文字配置、Dynamic Type 最大付近
- ✅ **日本語 LINE Seed を Google Fonts 公式版へ交換（行高の原因特定）**
  通常画面の間延びは、2024年 App 用 OTF（`LINESeedJP_A_OTF_*`）の上下メトリクスが大きいことが原因だった。文字本体の cap height は Google Fonts 版と同じで、ascender／descender だけが広い。
  - 先に `leading(.tight)` を試した。LINE Seed の leading は 0 で、ascender／descender には効かず、実機ではほぼ変化がなかった
  - Google Fonts 公式の `LINESeedJP-Regular.ttf` / `LINESeedJP-Bold.ttf`（上流 2025年版 v1.008）へ差し替え、tight leading は撤回した
  - 文字サイズ・semantic text style・画面 padding・spacing は変更していない
  - 英語・韓国語・繁体字の公式配布 OTF と、簡体字の PingFang SC 方針は維持
  - 設計メモ `docs/typography-design.md`
  - **人間タスク**: 日本語リスト／複数行／詳細・設定・Paywall・オンボーディング、Dynamic Type、欠け・重なり・タップ領域、共有 PNG の日本語ラベル、繁体字 UI の行高、App Store 提出前の各フォントライセンス確認
- ✅ **マンスリーログ共有の外部PNGを描画範囲へクロップ**
  Instagram Stories 用の 1080×1920 全体ではなく、X などで写真の上へ重ねやすいよう、外側の透明を切り詰めた。
  - 内部マスターは従来どおり 360×640・scale 3・1080×1920。テンプレートの配置は変えない
  - `MonthlyLogShareImageTrimmer` が既知の RGBA へ正規化して `alpha > 0` を走査し、四辺へ 24 物理ピクセルを足してクランプする
  - コピー・写真保存・共有シートは同じトリミング済み PNG Data。完全透明は失敗
  - プレビューの市松カードは固定 9:16 のまま、内部マスター（`MonthlyLogShareCanvas` 全体）を縮小表示する。トリミング済み PNG はプレビューに使わない
  - LINE Seed の描画・計測は維持
  - **人間タスク**: 実機の X で 2:3 写真へ重ねて拡大できること、Stories の貼り付け、写真アプリの透明保持、文字・影が切れていないこと、3経路の見た目一致
- ✅ **アプリ内タイポグラフィを LINE Seed へ全面移行（R4.6）**
  BookBank が描画する文字を、実効言語ごとの LINE Seed Regular/Bold に揃えた。簡体字だけは適切な簡体字形のため PingFang SC／システムフォントを使う。
  - 共通定義は `Utils/AppTypography.swift`（`Font.app` / `UIFont` + `UIFontMetrics`）。ファイル名ではなく OTF の PostScript 名を使用
  - SwiftUI UI、ナビバー、メモ編集、スプラッシュ／オンボーディング／Paywall、マンスリーログ共有画像を対象。SF Symbols と OS 所有画面は対象外
  - 共有画像の月名・曜日・ワードマークは LINE Seed Sans EN、ローカライズ文字は snapshot.locale。計測と描画は同じ固定フォント
  - 旧 Inter / Fearlessly Authentic 3ファイルと Info.plist 登録を削除。完成ワードマーク SVG は未提供のため文字で描く
  - スキーマ・DTO・リポジトリ・移行コードは未変更。設計メモ `docs/typography-design.md`
  - メモ編集は `beginEditing` 中に `textContainerInset` を変えないよう直した。LINE Seed の行高で先頭引用の inset が変わり、layoutManager が編集中に動いて落ちた
  - **人間タスク**: App Store 提出前の LINE Seed ライセンス文書確認（バンドル埋め込みと共有画像への焼き込み）
- ✅ **マンスリーログ共有を実装（R4.6「スクリーンショットの画像候補」）**
  本棚カレンダーをスクリーンショットした直後、または各月ヘッダーの共有ボタンから「マンスリーログを共有」画面を開き、対象月の本・金額・カレンダーで透明PNG 4種を作って選べるようにした。
  - 週始まりは `Calendar.current.firstWeekday` のまま。配置計算は `MonthlyCalendarLayout` にまとめ、通常画面と共有画像が同じ結果を使う
  - 共有画像の月名・曜日は英語固定（`MonthlyLogShareEnglishLabels`）。曜日の並び順だけ端末の週始まりに追従。金額・冊数・通貨単位と共有画面UIは5言語
  - 集計は `BookshelfView.passbookBooks` を月で絞る。新しい `observeBooks()` 購読は作らない。金額は既存の `totalDisplayAmount`
  - 表紙は共有画面を開いた時点で事前解決（`LocalCoverDataCache` → `loadCoverImage` → `BookCoverImageCache` → URL）。`ImageRenderer` 内で非同期ロードを待たない
  - コピーは PNG Data を `UTType.png` でクリップボードへ。保存は写真の add-only 権限。その他は一時PNGを共有シートへ（iPadはPopover）
  - `InfoPlist.xcstrings` を新設し、`NSPhotoLibraryAddUsageDescription` を5言語で管理。Xcode の `xcstringstool` は認識した
  - 設計メモ `docs/monthly-log-share-design.md`。参考画像は `docs/assets/monthly-log-share/`（Assets には入れない）
  - `xcodebuild test` 全件 **TEST SUCCEEDED**（exit 0・iPhone 17 / iOS 26.5）
  - **人間タスク**: 実機スクリーンショット検知、Instagram Storiesへの貼り付けと透明PNG保持、5言語の文字収まり、英語月名・曜日のデザイン確認、App Store提出前の写真権限文言
- ✅ **マンスリーログ共有の完了前補正**
  スクショ共有がカレンダーから離れたあとも起動しうる問題と、表紙準備の直列待ち、共有セルが正方形だった点を直した。
  - 自動表示は `showCalendarView` に加え、`BookshelfCalendarView` が実際に前面かを見る。詳細・検索・別タブ・既存モーダルでは出さない。準備完了直後にも前面を再確認し、離れたら Task をキャンセルして遅延表示しない
  - 表紙はキャッシュを即時解決し、ローカルは既存 `loadCoverImage`、リモートだけ最大4件並列。同じ URL は1回だけ取得。準備中は ProgressView（`monthly_log_share.preparing`・5言語）
  - 共有カレンダーの表紙・空・月初空白セルを 2:3 に揃え、4〜6行でも 1080×1920 の透明 PNG に収める
  - 判定は `MonthlyLogShareScreenshotGate` の純粋関数としてテスト固定
  - `xcodebuild test` 全件 **TEST SUCCEEDED**（exit 0・258件成功・0件失敗・iPhone 17 / iOS 26.5）

### 2026-08-15
- ✅ **リリース前の人間タスクをクローズ（オーナー確認）**
  R4.5の新規文言5言語レビュー、書籍詳細シートの実機確認と `book.delete.action` の訳、通帳／本棚検索の実機確認、メモ一覧・本文検索・タブバー背景の実機確認はいずれもOK。訳の差し戻しはなし。
- ✅ **本の詳細ページのタブバー背景を通帳ページと同じ透明表示へ修正**
  本の詳細だけSwiftUIの自動判定でタブバー背景が不透明になり、タブバー下の画面内容が透けて見えなかった。タブバー本体は残し、`.toolbarBackground(.hidden, for: .tabBar)` で背景だけを非表示にした。ヘッダー吸い付き型シートの構造や操作感は変更していない。
- ✅ **メモ一覧ページを新設（R4.6「メモのノートページ」）**
  本に書いたメモだけを一覧し、タップするとその本の詳細へ戻れる独立ページを追加した。「言葉のリストから本に辿れる」ことが目的。
  - 総合口座の「口座管理」導線を「メモ一覧」へ差し替え（`PassbookActionDestination.memos` → `MemoListView`）。口座タブと口座切り替えメニューの「口座一覧」は残置
  - 一覧は装飾を描画せず、既存の `MemoPlainText.stripped(from:)` でプレーン化した本文を全文表示する（表紙・書誌は出さない）
  - プレーン化後が空白・改行だけのメモは除外。並びは `observeBooks()` の正準順を維持
  - **本文検索を追加**——右上の虫眼鏡から、画面に出しているプレーン本文だけを1文字ごとに絞り込む（タイトル・著者・生メモは対象外。照合は既存の `ShelfSearchMatcher` を再利用）
  - 設計メモ `docs/memo-list-design.md` を新設。ロードマップ R4.6 と `docs/memo-tagging-design.md` 4.1節を実装後の仕様へ更新
  - 検証はビルド確認まで（テストは省略——見た目・遷移の変更で、プレーン化は既存純粋関数の再利用のみ）
- ✅ **個別に作った引用が保存後に1枠へ合体する問題を修正**
  引用ボタンを別々に押すと、数字未入力の `p.` 行が保存時に消えて引用行が隣り合い、詳細表示が1つの囲みにまとめてしまっていた。
  - 保存時、空の `p.` の直後が別の引用なら完全削除せず空行1つを境界として残す（`MemoQuotePage.removingEmptyPageLines`）
  - 詳細表示は、引用のあいだの空行1つを別枠の境界として解釈する（`MemoTextBlocks.parse`。空の本文ブロックは作らない）
  - 再編集時はその空行を前の引用の `p.` へ戻す（`restoringBoundaryPageLines`）。保存→再編集→再保存しても形が増えない
  - 複数行を1回で引用した形（空行なし）と、すでに保存済みの連続引用は従来どおり1枠（由来を判別できないため自動分割しない）
  - 設計メモ 4.6節を先に更新。`xcodebuild test` 全件 **TEST SUCCEEDED**（exit 0・iPhone 17 / iOS 26.5）

### 2026-08-13
- ✅ **書籍詳細のボトムシートを「ヘッダー吸い付き型」へ変更（R4.6・UIパッチ枠の1件目）**
  本の詳細ページの下半分のパネルを上へ引き上げると、画面のいちばん上まで伸びてナビゲーションバーと一体化するようにした（通帳画面のシートと同じ操作感。角丸・背景・ハンドルは展開度に応じて連続変化）。
  - 展開時のナビバーは「左：閉じるchevron／中央：その本の書影／右：ガラスの編集ボタン」。折りたたむと標準の戻るボタンへ戻る。右下の編集ボタンは展開時に消える
  - 右上にあった削除ボタンは、メモ欄の下の赤い「この本を削除」リンクへ移設（`book.delete.action` を5言語で追加。確認アラートと削除処理は従来のまま）
  - カレンダーで同じ日に複数冊ある日は、一覧シートで本を選ぶと**シートを閉じてから**通常の詳細画面を開くようにした（シートの中では画面最上端まで展開できないため。バグレビュー D-4 の後日変更として記録）
  - 検証はビルド確認まで（テストは省略——見た目・レイアウト・遷移のみの変更で、`Utils/` の純粋関数には触れていない）。**実機での見た目・操作感の確認と、削除文言の訳レビューが人間タスクとして残る**（R4.5の5言語レビューと同時に実施してよい）
- ✅ **ロードマップに R4.6（v1.6.x UIパッチ枠）を新設**
  R6（クラウド移行）は大型で期間が読みにくく、合間の割り込みUI変更が無記録で進んでいたため、「登録してから着手する」枠を作った。入場条件は①View層で完結しR6と干渉しない ②1件ずつv1.6.xのパッチとして出せる粒度 ③着手はオーナー指示があったものだけ。上のシート変更を1件目として登録し、候補に「メモのノートページ」「スクリーンショットの画像候補」を登録（いずれも仕様未定）。**ウィジェットはこの枠に入れず R6完了後**（App Groupのデータ受け渡しがFirestore移行で作り直しになるため。ロードマップ「将来フェーズ」に登録）
- ✅ **R4.5「メモつながり」T1の実装（ステップ5'〜9'・2026-08-11〜13）**
  メモの中に `[[京都]]` のように書くと、同じ言葉を書いた本同士が「つながり」になる機能。タグ欄のようなデータ項目は作らず、メモの文字列だけで成立する（設計は `docs/memo-tagging-design.md`）。
  - **ステップ5'（8/11）: パーサ置換** — 読み取りの仕組みを `#タグ` から `[[ ]]` へ差し替え（`MemoTag*` → `MemoLink*` にリネーム）。半角・全角の括弧どちらでも、混ざっていても認識する
  - **ステップ6'（8/11）: 表示** — 本の詳細画面のメモで、`[[ ]]` の記号を隠して中身だけテーマ色で表示。太字（`**`）・引用（行頭の `> `）・ページ番号（`p.数字` のバッジ）もここで描くようにした
  - **ステップ7'（8/11〜12）: メモ編集ツールバー** — メモ編集画面の下に「つなぐ・太字・引用・ページ番号・書式クリア・ひとつ戻す」の6ボタンを新設（5言語対応）。**R4.5に同乗させる予定だった「UI改善」の枠はここに充てた**——当初はバックログから別途選ぶつもりだったが、`#` を打たせる入力負担を解消するためにツールバーが要ることになり、それ自体が改善になった。編集中の画面でも記号を見せず、装飾がその場で反映される方式に差し替えた。実機確認を6回重ね、ボタンのトグル化・いまいる装飾のボタンが光るアクティブ表示・隠した記号をまたぐカーソルの規則の一本化など、指摘を反映して作り込んだ
  - **ステップ8'（8/12）: つながりチップと絞り込み** — 本の詳細につながりのチップ行を追加。タップすると総合口座の本棚に切り替わり、そのつながりを持つ本だけに絞り込まれる。本棚内検索を開くと（入力前に）つながりの一覧が件数付き・多い順で出る（上位20件＋もっと見る）。検索語に部分一致するつながりは検索結果の上にチップで出る。`[[京都]]` と `[[きょうと]]` のような書き分けは、この一覧で気づいて直す方針（入力サジェストは廃止）
  - **ステップ9'（8/13）: 検収** — 設計メモ・ロードマップ・本棚内検索仕様書の記述を実装後の姿に同期。既存メモに `[[ ]]` などが偶然含まれて意図しない装飾にならないかを実データで調べるチェックスクリプト（`tools/memo-markup-check.py`）を用意し、実機の実データで実行した（書籍421冊・うちメモあり16冊）。**検出52件はすべて意図した表示で、意図しない誤装飾はゼロ**。ただしメモを持つ本が少なく、検出された記法の大半は開発中に書かれたものなので、「大量の既存メモに対する安全性」までは担保していない（設計メモ 7.1節）
  - **リリース前に残る人間タスク: UI文言の5言語レビュー** — 実機で見たのは「文字が切れないか」という幅の話で、訳の妥当性は未確認。対象はツールバー6ボタンのラベル・つながり一覧の見出し・0件表示の文言・絞り込みチップまわりの新規文字列（ロードマップ R4.5「人間タスク（リリース前）」）
  - テストはユニットテスト全184件通過（iOS 26.5 / iPhone 17。iOS 26.2 のシミュレータではSwiftDataを使うテストが環境起因で落ちる既知事象があり、検収環境は26.5に固定）
- 🔒 **R1の残件 A-9 / B-5 をクローズ（オーナー判断）**
  A-9（検索で発行形態を絞っているとき、あとから形態が判明した本が一覧に出てこない）と B-5（Google Books の価格換算で1円ぶんの丸め誤差が起きうる）の2件。**どちらも直していないが、「直さない」という判断として閉じた**。R1（v1.3.1）から R2・R3・R4・R4.5 と5回のリリースを見送ってきて実運用で問題になっておらず、実害なしと判断したため。「保留」のまま持ち越すと申し送りが実態とずれていく。**症状の記述は `docs/bug-review-2026-07-06.md` に残してある**ので、将来同じ症状に気づいたときに既知か新規かを判別できる（実害の判断が変われば開け直す）

### 2026-03-01
- ✅ **初期設定の変更（デフォルトテーマ）**
  - デフォルトテーマカラーをゴールド（#918658）に変更（OnboardingView、AddPassbookView）
  - デフォルトの外観モードをライトモードに変更（ThemeManager）
  - 読了リスト作成時のデフォルトカラーもゴールドに統一（AddReadingListView）

- ✅ **黒テーマ判定の修正（全ビュー統一）**
  - 色順変更後に残っていた `colorIndex == 0` の古い判定を修正
  - PassbookDetailView、BookshelfView、MainTabView、StatisticsViewを `PassbookColor.isBlackTheme()` に統一
  - 初めて口座を作成した時に背景が黒になる問題を修正

- ✅ **読了リスト作成画面の改善（AddReadingListView）**
  - 本が1冊も登録されていない場合は「まずは口座に本を登録しましょう」メッセージを表示
  - テーマカラーのtintで「本を登録する」カプセル型ボタンを追加
  - ボタンタップで通帳タブに自動遷移（MainTabView連携）
  - テーマカラーをMainTabView → ReadingListView → AddReadingListViewに伝播

- ✅ **口座を管理ページの改善（PassbookSelectorView）**
  - 「このアプリについて」リンクを追加（https://ayame-inc.jp/products/bookbank）
  - 「新しい口座を追加」ボタンを下部固定エリアからScrollView内に移動（口座一覧ページと統一）

- ✅ **口座一覧ページの背景改善（AccountListView）**
  - bg_glowの配置をThemedBackgroundViewと同じ設定に統一（width 2.2倍、上部揃え）

- ✅ **読了リストページの背景追加（ReadingListView）**
  - bg_glowとbg_noiseをThemedBackgroundViewと同じ設定で配置

- ✅ **本の検索画面の改善（BookSearchView）**
  - 検索結果0件時の虫眼鏡アイコンを削除
  - 手動登録ボタンをカプセル型に変更、鉛筆アイコンを削除

- ✅ **バーコードスキャナーのガイド枠ずれ修正（BarcodeScannerView）**
  - ScanAreaOverlayとCornerBracketShapeの配置を統一（VStack→ZStack中央配置）

- ✅ **メモ欄のフォントサイズ変更（UserBookDetailView）**
  - .footnote（13pt）から.subheadline（15pt）に変更

- ✅ **Unlimited関連の色統一**
  - ダウンロード形式選択ページのUnlimitedバッジ・ボタンを#A1975Dに変更（MarkdownExporter）
  - Markdownプレビューカードのダークモード背景色を明るく調整（ヘッダー#323232、コード本体#232323）

- ✅ **UnlimitedPaywallViewの大幅リデザイン**
  - テーマカラー#A1975D、bg_paywall.png背景、ダークモード固定
  - 価格表示を「980円」形式に変更、機能アイコンをcheckmarkに統一
  - 買い切りプランをデフォルト選択、「おすすめ」バッジ追加
  - Unlimitedタイトルに白→ゴールドのグラデーション適用

- ✅ **カスタムカラーピッカーの改善**
  - カスタムカラー選択時に吹き出しデザインで視覚的に強調（Triangle shape）
  - デフォルトカラーパレットにゴールド（#918658）と緑（#145F00）を追加
  - カラーグリッドを5列→6列に変更
  - カラー順序を赤→オレンジ→黄色→黄緑→シアン→緑→青→紫→ピンク→黒→ゴールドに変更

- ✅ **口座一覧・口座管理の三点リーダーサイズ拡大**
  - ellipsisアイコンを10pt→14ptに変更

- ✅ **詳細エクスポート機能の実装**
  - PassbookSelectorView、ReadingListDetailViewの「詳細情報をダウンロード」ボタンを有効化

- ✅ **その他の改善**
  - 口座一覧のUnlimitedバッジを上方に配置、色をグラデーションからシステムセカンダリカラーに変更
  - 空チャートのグレー色を薄く調整
  - 金額グラフのY軸が0冊時に1になる問題を修正（StatisticsView）
  - 読了リスト詳細のテーマカラー選択をチェックマーク方式に変更

### 2026-02-27
- ✅ **有料プラン名を「Platinum」から「Unlimited」に全面変更**
  - PlatinumManager → UnlimitedManager（ファイル名・クラス名変更）
  - PlatinumPaywallView → UnlimitedPaywallView（ファイル名・構造体名変更）
  - PlatinumAlertView → UnlimitedAlertView（ファイル名・構造体名変更）
  - isPlatinum → isUnlimited（プロパティ名変更）
  - UI表示テキストをすべて「Unlimited」に統一（バッジ、ボタン、アラートメッセージ等）
  - Products.storekitの表示名を「Unlimited（年額）」「Unlimited（買い切り）」に変更
  - 対象ファイル: UnlimitedManager, UnlimitedPaywallView, UnlimitedAlertView, AccountListView, PassbookSelectorView, MainTabView, MarkdownExporter, Products.storekit

- ✅ **BookSearchView（本の検索ページ）のFABボタン非表示**
  - 検索ページではタブバーとFABボタンを非表示に（`.toolbar(.hidden, for: .tabBar)`）

- ✅ **AccountListView（口座一覧ページ）のグラフ改善**
  - 口座はあるが0冊の時もグレーのグラフを表示
  - 口座が1つもない時もグレーのプレースホルダーグラフを表示
  - パーセンテージラベルは実データがある時のみ表示

- ✅ **ReadingListView（読了リスト一覧）の改善**
  - カードの境界線を削除
  - サムネイル表示をReadingListDetailViewと統一（5カラム×最大2行、画像なしはスキップ）

- ✅ **ReadingListDetailView（読了リスト詳細）のダークモード改善**
  - シェアプレビューのURL背景色をダークモードで視認可能に（`secondarySystemGroupedBackground`）
  - シェアプレビューのサムネイルグリッド下段の本が見切れる問題を修正（動的aspectRatio）
  - ダークモード時のシェアプレビューボトムシート背景を黒に変更

- ✅ **AddPassbookView（口座追加ページ）のデザイン統一**
  - カテゴリタグの幅を拡大
  - カテゴリタグセクションを左寄せに変更
  - 入力フィールドの高さをOnboardingViewと統一（50pt）
  - テーマカラーセクションの位置を下方に調整

- ✅ **EditPassbookView（口座編集ページ）のデザイン統一**
  - 入力フィールドをAddPassbookView / OnboardingViewと同じスタイルに変更
  - フォント18pt light、中央揃え、高さ50pt、角丸4pxの枠線
  - 「口座」ラベルを入力フィールドの右に追加
  - セクションラベルのフォントを`.headline`に統一

### 2026-02-26
- ✅ **OnboardingView（オンボーディング画面）のレイアウト刷新**
  - レイアウト崩れを根本修正（背景画像のfillによるZStackサイズ膨張を解消）
  - 背景画像をOnboardingBackground.jpgに変更（imageset化、SplashScreenViewと同じ配置方式）
  - デフォルトで「小説」を選択状態に
  - カテゴリタグの選択状態を白背景＋黒テキストに変更
  - サブタイトルに文字間（kerning: 3）を追加
  - キーボード表示時にスクロール対応（セクション間の余白が詰まらない）
  - キーボード外タップでキーボードを閉じる機能を追加

- ✅ **AddPassbookView（口座追加画面）のカテゴリタグ改善**
  - OnboardingViewと同じ14個のカテゴリ候補に統一
  - タグデザインをOnboardingViewと統一（選択時: ベタ背景＋反転色、非選択時: 境界線のみ）
  - FlowLayoutから手動行分割に変更

- ✅ **BookshelfView（本棚ページ）に月別カレンダービューを追加**
  - 右上にカレンダーアイコンボタンを追加（icon-calendar.imageset）
  - タップで通常グリッド⇔月別ビューを切り替え
  - 月別7カラムグリッドで本のサムネイルを表示
  - 「2026年1月」のような日本語表記で年月を表示（フォントはtitle3）
  - 月ごとの冊数も表示
  - 月と月の間隔は50pt

- ✅ **ReadingListDetailView（読了リスト詳細）の大幅改善**
  - サムネイルグリッドを本棚デザインからシンプルな5x2グリッドに変更
  - シェアボタンをアイコン+テキスト形式に変更（icon-share.imageset）
  - ダウンロードボタンを削除（三点リーダー内に統合済み）
  - 並べ替えボタンのアイコンをicon-sortに変更
  - 三点リーダーをシステムアイコン（ellipsis）に変更
  - 三点リーダー内のアイコンをカスタムアイコンに変更（icon-share、icon-edit、icon-delete）
  - 金額と冊数を右寄せに変更、順序を入れ替え
  - 金額にグラデーションを適用、フォントweightをMediumに
  - List Contentの各リストに背景色を追加
  - List Contentのbottomに40ptの余白を追加
  - 削除アイコンの色をシステムカラー（赤）に変更
  - 削除アラートのキャンセルボタンの色を非テーマカラーに変更

- ✅ **SharePreviewSheet（シェアプレビュー）の改善**
  - 背景をThemedBackgroundView風のデザインに変更
  - サムネイルグリッドをシンプルな5x2グリッドに変更
  - List Contentに背景色を追加
  - プレビューカードの上にシェアURLとコピーボタンを追加
  - アクションボタン（リンクをコピー、ブラウザで表示、Xでシェア）を削除
  - 閉じるボタンの二重丸を修正

- ✅ **BookSelectorView（本選択画面）の改善**
  - List Contentの各リストに背景色を追加
  - スクロール最下部の背景色を透明に変更
  - 本のサムネイル表示をCachedAsyncImageに変更（表示バグ修正）

- ✅ **AddReadingListView（読了リスト作成）の改善**
  - 本選択UIをBookSelectorViewに統一（スワイプ式）

- ✅ **ReadingListView（読了リスト一覧）の改善**
  - 削除アラートのキャンセルボタンの色を非テーマカラーに変更

- ✅ **アセット追加**
  - icon-calendar.imageset（カレンダービュー切り替え用）
  - icon-share.imageset（シェアボタン用）
  - icon-edit.imageset（編集ボタン用）

- ✅ **AccountListView（口座一覧ページ）のクラッシュ修正**
  - 円グラフのパーセンテージ計算で0除算・NaN/Infiniteチェックを追加
  - Double→Int変換時のクラッシュを防止

- ✅ **BookSearchView（本の検索ページ）のトースト通知改善**
  - 黒テーマ+ダークモード時のテキスト色を黒に変更
  - 白背景+白テキストで見えなくなる問題を修正

- ✅ **BookCoverView（本の表紙）のバッジデザイン変更**
  - 背景を透過なし白背景に変更
  - アイコン色を黒に変更
  - アイコン間のスペースを2ptに変更

- ✅ **StatisticsView（集計ページ）の大幅改善**
  - 年別統計サマリー（2x2グリッド）を追加：合計金額、冊数、お気に入り数、メモ数
  - 統計サマリーをグラフと一緒にスワイプ可能に
  - 数値にテーマカラーのグラデーションを適用
  - 単位（円/冊）のフォントを小さく
  - 金額グラフにAreaMark（グラデーション塗りつぶし）を追加
  - 冊数の棒グラフにグラデーションを適用
  - Y軸ラベルに固定幅50ptを設定（縦ラインの位置を揃える）
  - 口座サマリーに「口座サマリー」タイトルを追加
  - ドットインジケーターとグラフの重なりを解消
  - 最下部のFABボタンとの重なりを解消（80ptパディング追加）

### 2026-02-25
- ✅ **PassbookDetailView（通帳ページ）のコンパクトヘッダー実装**
  - スクロールに連動して金額を縮小表示するコンパクトヘッダーを追加
  - ボトムシートのハンドルがスクロールでフェードアウト
  - コンパクトヘッダーは画面上部に固定、opacityでフェードイン
  - 戻るボタン（chevron.down）でスクロール位置をリセット
  - 戻るボタンのアイコンカラーをテーマカラーに変更
  - 金額にテーマカラーベースのLinearGradientを適用

- ✅ **ThemeToggleButton（テーマ切替ボタン）の改善**
  - アイコンカラーをテーマカラーからiOS標準（accentColor）に変更

- ✅ **StatisticsView（集計ページ）の改善**
  - 左上の年数表示を白色に変更
  - 金額グラフのY軸を0以上に制限（マイナス値を非表示に）

- ✅ **AccountListView（口座一覧ページ）の改善**
  - 「新しい口座を追加」ボタンの境界線・塗りを削除
  - グラフと口座リストの間隔を16pt追加
  - 背景にbg_glow.png（opacity 0.3）とbg_noise.png（overlay、opacity 0.2）を追加
  - 光源画像を上部中央に配置

- ✅ **PassbookSelectorView（口座を管理ページ）の改善**
  - 「新しい口座を追加」ボタン上のDividerを削除

- ✅ **UserBookDetailView（本の詳細ページ）の大幅改善**
  - ボトムシート風デザインに変更（角丸40px、ハンドル付き）
  - お気に入りボタンをカバー画像の右下に配置
  - 黒テーマ+ダークモード時のお気に入りアイコン色を黒に
  - 金額を右寄せ、フォントweightをmediumに
  - detailSectionの上に境界線を移動
  - メモ枠に薄い境界線を追加（ライトモード対応）

- ✅ **MemoEditorView（メモ編集画面）の改善**
  - 左右の内側余白を20ptに変更

- ✅ **BookshelfView（本棚ページ）のフィルターUI刷新**
  - フィルターボタンを「All [23]」「Like [12]」「Memo [5]」スタイルに変更
  - シンプルな線のみのカプセルボタンデザイン
  - 各フィルターに該当する書籍数を表示
  - 0件の場合は数字を非表示

- ✅ **BookCoverView（本の表紙）のバッジ改善**
  - メモ・お気に入りバッジを個別の角丸2px四角背景に
  - メモアイコンをicon-memoカスタムアイコンに変更

- ✅ **PassbookColor（口座カラー）の機能追加**
  - isBlackTheme()メソッドを追加（黒テーマ判定）

- ✅ **アセット追加**
  - icon-memo.imagesetのContents.jsonを作成

### 2026-02-24
- ✅ **ThemedBackgroundView（共通背景コンポーネント）の作成**
  - 通帳・本棚・集計ページの背景を再利用可能なコンポーネントに抽出
  - RadialGradient（テーマカラー→透明）、光源PNG、ノイズテクスチャを含む
  - themeColorとisBlackThemeをパラメータ化し、ダークモード時の黒テーマ対応

- ✅ **PassbookDetailView（通帳ページ）の背景改善**
  - テーマカラーのベタ塗りからRadialGradientベースのリッチな背景に変更
  - 光源PNG（bg_glow）とノイズテクスチャ（bg_noise）を追加
  - 金額テキストに白→テーマカラーのLinearGradientを適用
  - アクションボタン（本を登録する・本棚を見る）を追加
  - リスト項目にテーマカラーの背景と角丸を適用

- ✅ **BookshelfView・StatisticsViewへの背景適用**
  - 両ページの背景をThemedBackgroundViewに統一
  - isBlackThemeプロパティを追加し、白テーマ＋ダークモード対応

- ✅ **LiquidGlassButtonの改善**
  - 白テーマ＋ダークモード時は塗りを白、テキストを黒に反転
  - isBlackThemeパラメータを追加

- ✅ **AccountListView（口座一覧）の改善**
  - 口座ごとの金額テキストをテーマカラーで表示
  - 「新しい口座を追加」ボタンのアイコンを統一

- ✅ **ReadingListDetailView（読了リスト詳細）のダークモード対応**
  - ダークモード時に背景にColor.black.opacity(0.2)を追加

### 2026-02-22
- ✅ **ReadingListDetailView（読了リスト詳細）の改善**
  - サムネイルグリッドの左右余白を24pt→12ptに変更（本を大きく表示）
  - カルーセルのカード幅を65%→75%に変更（アクティブカードを大きく）
  - カルーセルのカード間スペースを16pt→8ptに変更
  - カルーセルのカード内に水平16ptのpaddingを追加
  - カルーセル上の説明文を全文表示（lineLimit解除、fixedSize追加）
  - 説明文とカルーセルの間隔を16pt追加

- ✅ **Platinum誘導ポップアップのデザイン統一**
  - PlatinumAlertView.swiftを新規作成（共通コンポーネント）
  - 「Platinum機能を体験する」ボタン: 青塗り（.blue）+ 白文字
  - 「キャンセル」ボタン: 黒テキスト（.primary）
  - 全箇所で統一（AccountListView、PassbookSelectorView、MainTabView、MarkdownExporter）
  - .confirmationDialog から カスタムオーバーレイに変更

- ✅ **エクスポートプレビュー（ExportSheetView）の改善**
  - 詳細エクスポートのプレビューに全フィールドを表示
  - 著者、金額、出版社、登録日、ISBN、表紙画像、メモの7項目
  - データがない場合はサンプルテキストを表示

### 2026-02-15
- ✅ **口座選択画面の変更（PassbookSelectorView）**
  - タイトルを「口座を選択」→「口座を管理」に変更
  - 「新しい口座を追加」の下に「サブスクリプションを管理」リンクを追加（App Storeのサブスク管理URLを開く）
  - 表示条件: isPlatinum かつ年額プランの場合のみ（lifetimeは非表示）
  - PlatinumManager に hasActiveYearlySubscription を追加

- ✅ **読了リスト一覧（ReadingListView）のレイアウト変更**
  - 2カラム→1カラムに変更
  - 各カードの表紙を3x3グリッド→5カラム1行（横並び・最大5冊）に変更

- ✅ **読了リスト詳細（ReadingListDetailView）のUI調整**
  - 本棚グリッドとタイトルの間隔を16pt→24ptに拡大
  - リストビューの過剰な余白を解消（コンテンツカードの minHeight を削除）
  - アクションボタン: 三点リーダーの左にシェア・ダウンロードのアイコンボタンを追加

- ✅ **BookCarouselView を映画チケットアプリ風に変更**
  - 各本を白いカードで表示（角丸16pt、シャドウ）
  - 中央カード scale 1.0 / opacity 1.0、左右は scale 0.85 / opacity 0.4 でチラ見せ
  - スワイプでスナップ、spring アニメーション
  - 背景は半透明の黒のまま
  - カード間スペースを8ptに縮小、表紙を DESIGN_SYSTEM 準拠で 140x210px、カルーセルエリア幅を100%に変更

### 2026-02-11
- ✅ **StoreKit 2ベースの課金機能（Platinum）の実装**
  - Products.storekit を新規作成（年額 980円 / 買い切り 1,200円）
  - PlatinumManager.swift を新規作成（@Observable、StoreKit 2）
  - Transaction.currentEntitlements で購入状態復元、Transaction.updates でリアルタイム更新
  - purchase(product:) / restorePurchases() を実装
  - isPlatinum は yearly または lifetime のいずれかが有効なら true

- ✅ **既存のPro制限を PlatinumManager.isPlatinum で判定に変更**
  - 口座4つ以上の作成制限（AccountListView / PassbookSelectorView）
  - 読了リスト4つ以上の作成制限（MainTabView）
  - 詳細データのマークダウンダウンロード（MarkdownExporter）
  - 表記を「Pro」から「Platinum」に統一

- ✅ **PlatinumPaywallView の新規作成**
  - 銀行のプラチナカードをイメージしたダークトーンの課金画面
  - 特典説明（口座無制限・読了リスト無制限・詳細エクスポート）
  - 年額/買い切りをカード形式で横並び選択、購入・復元ボタン
  - 制限アラートから「Platinum機能を体験する」でシート表示

- ✅ **口座一覧での Platinum 会員表示**
  - AccountListView の左上に Platinum 時のみバッジ表示
  - 口座アイコン1つ + 「Platinum」テキストを Platinum カラー（グラデーション）で表示、枠線なし

### 2026-02-02（DAY72）
- ✅ **デザインシステム（DESIGN_SYSTEM.md）の作成**
  - 既存コードからデザインルールを抽出しドキュメント化
  - カラー、タイポグラフィ、間隔、角丸、影、コンポーネントの使い分けを定義
  - 背景2色構成パターンを追記

- ✅ **本の表紙デザインの統一**
  - 全画面で角丸を2pxに統一（BookSearchView、BookSelectorView、ReadingListDetailView等）
  - アスペクト比を2:3に統一
  - サイズを3パターンに整理（リスト: 50x75px、グリッド: 幅×1.5、単体: 140x210px）

- ✅ **リストビューの間隔統一**
  - 水平パディングを24pxに統一（PassbookDetailView、BookSelectorView等）
  - 垂直パディング8px、スペーシング0で統一

- ✅ **ダークモードの色調整**
  - Color.appGroupedBackgroundを#000000（純黒）に変更
  - ReadingListDetailViewの背景を2色構成に変更（上部: #1A1A1A、下部: #000000）
  - ReadingListDetailViewの棚板をダークモードで黒に変更
  - ReadingListDetailViewのリストビュー背景をダークモードで黒に変更

- ✅ **ReadingListDetailViewの改善**
  - 上段・下段の本棚の間隔を16pxに拡大
  - 下段の本棚をセンタリング
  - スクロール時の背景処理を改善（2色構成で浮きを防止）
  - 編集モーダルのキャンセルボタンの色を削除、保存ボタンを変更時のみアクティブに

- ✅ **本棚ページ（BookshelfView）の改善**
  - 上部の金額セクションを削除
  - フィルターボタンを追加（お気に入り、メモあり）
  - コンテンツカードの角丸と背景を削除
  - 背景色をテーマカラー（opacity: 0.1）に変更
  - お気に入りアイコンをカスタムアイコン（icon-favorite）に統一

- ✅ **本の詳細画面（UserBookDetailView）の改善**
  - 画像なし時のアイコンを削除（「画像なし」テキストのみ残す）

### 2026-02-02（DAY72続き）
- ✅ **口座編集画面（EditPassbookView）の改善**
  - キャンセルボタンの色を削除（.primary）
  - 保存ボタンを変更時のみアクティブに（hasChanges検知）
  - Formを自前レイアウトに変更（口座名入力の角丸16px、境界線追加）
  - キーボード外タップで閉じる機能を追加
  - 削除ボタンを角丸12px＋境界線デザインに変更

- ✅ **マークダウンダウンロード機能の実装**
  - MarkdownExporter.swiftを新規作成
  - MarkdownDocument（FileDocument準拠）でファイル出力対応
  - 口座・読了リストをMarkdown形式でダウンロード可能に
  - タイトルと著者名のみ（無料）/ 詳細情報（Pro）の2段階ダウンロード
  - VSCode風プレビューカード（シンタックスハイライト風）
  - 実際の登録データからプレビュー表示
  - Pro版: 表紙画像URL、メモ、お気に入りフラグも出力

- ✅ **ダウンロードUI（ExportSheetView）**
  - ボトムシート形式でプレビュー表示
  - VSCode風コードブロック（ダーク背景、3つのドット、markdownラベル）
  - タイトルと著者名のみ / 詳細情報を含めるの選択
  - Proボタンにグラデーション（#2280e2→#fd7020）
  - ダークモード対応（背景を黒に）
  - 「エクスポート」表現を「ダウンロード」に統一

- ✅ **読了リストのアクションボタン整理**
  - 「追加」「並べ替え&削除」と三点リーダーの3ボタン構成
  - 三点リーダーでボトムシート表示（MoreActionsSheet）
  - シェア / ダウンロード / 名前と詳細の編集 / このリストを削除

- ✅ **読了リスト詳細画面（ReadingListDetailView）の改善**
  - リスト形式を通帳ページと同じスタイルに変更（右側に金額表示）
  - 背景色をsystemGroupedBackground（#1C1C1E）に変更
  - 「このリストを削除」機能を追加

- ✅ **カスタムアイコンの追加**
  - icon-download.svg（ダウンロードボタン用）
  - icon-more.svg（三点リーダー用）

- ✅ **デザインシステムの更新**
  - ツールバーボタンのルール追加（キャンセル色なし、保存は変更時のみアクティブ）
  - キーボード関連ルール追加（外タップで閉じる、レイアウト崩れ防止）

- ✅ **Pro機能制限の追加**
  - 口座一覧ページ・口座選択ページで4つ目以降の口座作成時にPro案内を表示
  - 外側タップで閉じるconfirmationDialogを使用

- ✅ **画像表示の安定化（CachedAsyncImage）**
  - CachedAsyncImage.swiftを新規作成
  - URLSessionで画像を手動ロードし、@Stateで保持
  - タブ切り替え時の画像消失問題を解決
  - BookCoverView、PassbookDetailViewで使用
  - BookshelfViewのLazyVGridに.animation(nil)を追加

- ✅ **本の検索ページの改善**
  - リスト境界線を削除、間隔をDESIGN_SYSTEMに準拠
  - 「未登録のみ」を「登録済みを除外」にテキスト変更
  - 「登録済み」バッジをオーバーレイ表示に変更（レイアウトシフト防止）
  - パフォーマンス改善（ソート結果キャッシュ、登録済みISBNのSet化）
  - 無限スクロールを「次の30件を読み込む」ボタン方式に変更

- ✅ **統計ページの改善**
  - お気に入り数、メモ数の表示を追加

- ✅ **Web共有機能の実装**
  - ShareService.swiftを新規作成
  - POST API連携（https://bookbank-share.vercel.app/api/lists）
  - readingListIdを送信し、同じリストから毎回同じURLを返すように対応
  - エラーハンドリング（ネットワークエラー、APIエラー）

- ✅ **シェアプレビュー画面（SharePreviewSheet）の実装**
  - iOS標準シェアシートからカスタムプレビュー画面に変更
  - プレビューカード（リストタイトル、合計金額、冊数、本のリスト最大4冊）
  - ライト/ダークモード対応
  - 「リンクをコピー」ボタン（コピー後フィードバック表示）
  - 「Xでシェア」ボタン（intent/tweetでX投稿画面を開く）
  - presentationDetents([.medium, .large])で引き上げ可能に

- ✅ **読了リスト画面の画像表示安定化**
  - ReadingListView、ReadingListDetailViewでCachedAsyncImageを使用
  - タブ切り替え時の画像消失問題を解決

- ✅ **本棚ページの空状態改善**
  - 「本棚はまだ空です」の上の本棚アイコンを削除

- ✅ **バーコードスキャナーの改善**
  - ガイド枠のアライメントを修正

- ✅ **本棚ページの改善**
  - フィルターボタンのテキストサイズを13pxに変更
  - 非選択時の背景を白、テキストを黒に変更

- ✅ **本の詳細ページの改善**
  - メモ欄の背景色をsecondarySystemGroupedBackgroundに変更

### 2026-01-31（DAY70続き）
- ✅ **読了リスト詳細画面（ReadingListDetailView）の大幅改善**
  - 本棚グリッドに奥行き表現を追加（斜め上から見下ろすデザイン）
  - 上段は棚板が薄く・小さく、下段は濃く・大きく
  - 棚板の高さ: 6pt→10pt、厚み: 2pt→4pt（上段→下段）
  - シャドウの強さ・長さも行ごとに変化
  - ダークモード対応: 棚板を白に、シャドウを白い光に反転
  - listInfoSectionの背景色・角丸を削除（シンプル化）
  - listContentの背景色をappCardBackgroundに変更（ダークモード対応）
  - listContentのパディング調整（top: 16px、左右: 24px）
  - スクロール最下部の背景処理を改善（100pxの余白追加）
  - 本棚とリストタイトルの間隔を狭く調整

- ✅ **本選択画面（BookSelectorView）の改善**
  - グリッド形式からリスト形式に変更
  - 口座別にスワイプで切り替え可能に
  - スティッキーヘッダーに読了リストのタイトルを表示
  - キャンセル・追加ボタンをヘッダーに統合
  - 日付フォーマットをYYYY.MM.DDに統一
  - チェックボタンのサイズ・境界線を調整
  - リスト間の境界線を削除（デザインシステム統一）

- ✅ **読了リストのテストデータ自動生成**
  - BookBankApp.swiftにgenerateReadingListTestData()を追加
  - 「2024年ベスト」「技術書まとめ」「おすすめ小説」の3リストを自動生成
  - 既存データがあっても読了リストがなければ自動追加

- ✅ **デザインシステムの統一（.cursorrules更新）**
  - リストビューの境界線は一切不要
  - 本の表紙は角丸2px
  - 日付フォーマットはYYYY.MM.DD
  - カード/ウィジェットはappCardBackground使用
  - アイコンサイズは14-18pt

### 2026-01-31（DAY70）
- ✅ **読了リスト機能（Myリスト）の実装**
  - Spotifyのプレイリストのような機能を新規追加
  - 本棚から好きな本を選んで「読了リスト」を作成できる
  - リストは複数作成可能

- ✅ **ReadingListモデルの作成**
  - `Models/ReadingList.swift`を新規作成
  - title, listDescription, createdAt, updatedAtプロパティ
  - UserBookへの多対多リレーション（本が削除されるとリストからも消える）
  - bookCount, totalValue, displayTotalValueの計算プロパティ
  - UserBookに`readingLists`逆参照を追加
  - BookBankApp.swiftのスキーマにReadingListを追加

- ✅ **リスト一覧画面（ReadingListView）**
  - 2カラムのカード形式で表示
  - 3x3グリッドの本の表紙サムネイル（2:3の本のアスペクト比）
  - タイトル・冊数・合計金額を表示
  - iOSウィジェット風の境界線
  - 長押しで削除可能
  - 右上にThemeToggleButton

- ✅ **リスト作成画面（AddReadingListView）**
  - Spotifyスタイルのステップ形式UI
  - Step 1: リスト名入力（デフォルト名「Myリスト#1」自動設定）
  - Step 2: 作成後、本を追加する画面に自動遷移
  - 「スキップ」で本を追加せずに完了も可能

- ✅ **リスト詳細画面（ReadingListDetailView）**
  - 本棚形式（4カラムグリッド）で本を表示
  - 合計金額・冊数をヘッダーに表示
  - 「本を追加」ボタンで本選択画面へ
  - 長押しで「リストから削除」（本棚からは削除されない）
  - 編集ボタンでタイトル・説明の編集、リスト削除が可能

- ✅ **本の詳細カルーセル（BookCarouselView）**
  - 本をタップするとカルーセル形式のポップアップ表示
  - ZStackオーバーレイで背景透過対応（thinMaterial）
  - GeometryReaderでカード幅を65%に設定（前後の本が見える）
  - scrollTargetLayout + scrollTargetBehaviorでスナップ動作
  - scrollTransitionで非アクティブなカードを縮小表示
  - initで初期スクロール位置を設定（2冊目以降も正しく中央表示）
  - 角丸24px、iOSウィジェット風の境界線、シャドウ付き

- ✅ **本選択画面（BookSelectorView）**
  - 全口座の本を4カラムグリッドで表示
  - 複数選択可能（チェックマーク表示）
  - 既にリストにある本はグレーアウト
  - 「完了」ボタンでまとめて追加

- ✅ **タブバーの更新**
  - 「Myリスト」タブを追加（5タブ構成に）
  - タブ順序: 口座 → 通帳 → 本棚 → 集計 → Myリスト
  - カスタムアイコン（icon-tab-mylist）を追加

- ✅ **Myリストタブの+ボタン改善**
  - Myリストタブでも右下の+ボタンを表示
  - タップするとMenuで「本を登録する」「読了リストを作成する」を選択可能
  - アイコンはタブバーと同じカスタムアイコンを使用

### 2026-01-29（DAY69）
- ✅ **ダークモード対応（ThemeManager）**
  - ThemeManager（@Observable）を新規作成、UserDefaultsで設定を永続化
  - AppTheme enum（system/light/dark）でテーマ状態を管理
  - カスタム背景色（Color.appGroupedBackground, Color.appCardBackground）を定義
  - ダークモード時はダークグレー系カラー（#1C1C1E, #2C2C2E）を使用
  - BookBankApp.swiftで`.preferredColorScheme`を適用
  - 全画面のColor(UIColor.systemBackground)等をカスタムカラーに置き換え

- ✅ **テーマ切替ボタン（ThemeToggleButton）**
  - ナビゲーションバー右上にテーマ切替メニューを配置
  - 通帳・本棚・集計・口座一覧の全タブに追加
  - SF Symbolsアニメーション付きアイコン切替（sun/moon/circle）

- ✅ **タブバーアイコンをオリジナルSVGに変更**
  - icon-tab-account、icon-tab-passbook、icon-tab-bookshelf、icon-tab-statisticsを追加
  - SF Symbolsから独自アセットに差し替え

- ✅ **削除アイコンをオリジナルSVGに変更**
  - icon-deleteアセットを追加（imageset + Contents.json）
  - UserBookDetailViewの削除ボタンをカスタムアイコンに差し替え

- ✅ **口座テーマカラーの適用範囲拡大**
  - 本検索画面：口座セレクタ・入金トースト・価格表示に口座カラーを反映
  - 手動登録画面：口座セレクタ・保存ボタンに口座カラーを反映
  - 本の詳細画面：口座テーマカラーを取得する仕組みを追加

- ✅ **UIの細かい調整**
  - LiquidGlassButtonのプラスアイコンを18pt→12ptに縮小
  - 通帳画面の日付フォントを.caption→.system(size: 10)に変更
  - 口座一覧・口座選択の三点リーダーを.system(size: 10)に変更
  - 口座行のpadding調整（trailing: 16→8）
  - 集計画面の背景をテーマカラーの薄い色に変更
  - 折れ線グラフの線幅を2→1に変更
  - 棒グラフにchartYScale(includesZero: true)を追加
  - 本棚グリッドに角丸2pxのclipShapeを追加
  - 手動登録フォームのセクション構成を変更（各項目を独立セクションに）
  - トースト通知のデザインを簡素化（アイコン削除、フォント縮小）

### 2026-01-29（DAY69）
- ✅ **ダークモード対応の改善**
  - ダークモード時の背景色を純粋な黒（#000000）に変更
  - 通帳・本棚ページのコンテンツ背景をColor(.systemBackground)に変更
  - スクロールバウンス時に背景が浮かない対策（GeometryReader使用）

- ✅ **口座テーマカラーの刷新**
  - 9色のカラーパレットに変更（赤、オレンジ、黄、緑、シアン、青、紫、ピンク、グレー/白）
  - 最初の色はライト/ダークモードで自動切り替え（ライト: 黒、ダーク: 白）
  - カラー選択UIを1列表示に変更
  - 新規口座追加ページにもカラー選択を追加

- ✅ **口座アイコンの改善**
  - fill + stroke の2層構造で表現（fill: opacity(0.1)、stroke: テーマカラー）
  - icon-tab-account-fill.imageset を新規追加

- ✅ **UIの統一・改善**
  - アプリ全体のフォント太字を削除（レギュラーに統一）
  - 三点リーダーのタップエリア拡大（44x44）
  - 「新しい口座を追加」ボタンの色をprimaryに変更
  - LiquidGlassButton を iOS標準 glassEffect に変更
  - トースト通知を glassEffect でリキッドガラス化

### 2026-01-26（DAY68）
- ✅ **スプラッシュスクリーン（SplashScreenView）の実装**
  - カスタムオープニングアニメーション付きスプラッシュ画面を作成
  - ドット → BookBank → your mind. → 四隅テキストの順にフェードイン
  - GeometryReaderでセーフエリアを考慮した配置
  - 6種類の背景画像からランダムに選択される機能
  - LaunchScreen.storyboardを削除し、SwiftUIベースに移行

- ✅ **カスタムフォントの導入**
  - 「Fearlessly Authentic」フォントを追加（ロゴ用）
  - 「Fearlessly Authentic Italic」フォントを追加（タグライン用）
  - 「Inter-Regular」フォントを追加（四隅テキスト用）
  - Info.plistにフォント登録

- ✅ **集計ページの改善**
  - 資産ポートフォリオセクションを削除

- ✅ **口座一覧ページの改善**
  - 円グラフのドーナツの太さを調整（innerRadius: 0.6）

### 2026-01-25（DAY67）
- ✅ **口座テーマカラー選択機能の実装**
  - Passbookモデルに`colorIndex: Int?`プロパティを追加
  - PassbookColorを8色から12色に拡張（red, yellow, teal, brownを追加）
  - 口座編集画面にカラーパレットUIを追加（グリッド表示で選択）
  - AccountListView、PassbookSelectorViewでcolorIndexを反映
  
- ✅ **口座一覧ページの円グラフ改善**
  - 総資産・金額・冊数を円グラフの中央に配置
  - グラフの高さを160px→220pxに拡大
  - ドーナツの太さを細く調整（innerRadius: 0.6→0.75）
  - 線の間隔を調整（angularInset: 1.5）
  
- ✅ **手動登録画面（AddBookView）の改善**
  - キャンセルボタンの色を削除（.primary）
  - 口座名の色を削除（.tint(.primary)）
  - 入力エリア以外タップでキーボードを閉じる機能
  - メモとお気に入りセクションを削除
  - セクションヘッダーを.footnoteに変更
  
- ✅ **本検索画面（BookSearchView）の改善**
  - 右上の鉛筆アイコンを「手動登録」テキストに変更
  - フォントサイズを.footnote、色を.primaryに変更
  
- ✅ **全ページのNavigationTitleフォント統一**
  - BookBankApp.swiftでUINavigationBarAppearanceを設定
  - 全ページのタイトルを.subheadline相当（15pt）に変更
  
- ✅ **ページタイトルの変更**
  - 通帳ページ：「BookBank」→「通帳」
  - 本棚ページ：「BookBank」→「本棚」
  
- ✅ **口座選択・一覧ページの細かいUI調整**
  - 色を表す●を12px→8pxに変更
  - 口座名のフォントを.body→.subheadlineに変更
  - 三点リーダーを.body→.captionに変更
  - グラフ中央の「総資産」を.headline、色を.primaryに変更

### 2026-01-24（DAY66）
- ✅ **楽天Books API を総合検索APIに変更**
  - 書籍専用API（BooksBook/Search）から総合検索API（BooksTotal/Search）に変更
  - コミック・雑誌も検索可能に（booksGenreId="001"で本カテゴリに絞る）
  - formatVersion=2でシンプルなJSON形式に対応
  - タイトル・著者の並行検索からキーワード検索に一本化（検索精度向上）
  - `RakutenTotalBookItem`モデルを追加し、`RakutenBook`に変換する仕組みを実装
  
- ✅ **UserBookDetailViewのカスタムヘッダー実装**
  - 標準のNavigationBarを非表示にし、自前のヘッダーを実装
  - `.ultraThinMaterial`背景でガラス風エフェクトを維持
  - 戻るボタンを自前で実装（chevron + "戻る"テキスト）
  - safeAreaInsetでヘッダーを配置（ノッチ対応）

### 2026-01-22（DAY64）
- ✅ **口座削除機能の実装**
  - PassbookDetailViewに口座編集機能を追加
  - 編集モーダルに「この口座を削除」ボタンを配置
  - 削除確認アラート実装（登録書籍数を表示）
  - 「この口座に登録されている○冊の本も削除されます」とアナウンス
  - Passbookモデルのリレーションシップを`.cascade`に変更
  - 削除後は自動的に口座一覧画面に戻る
  
- ✅ **楽天Books API検索の精度改善**
  - キーワード検索を追加（広範囲検索）
  - 検索結果が関連性の低い本を含む問題を修正
  - キーワード検索を削除し、タイトル・著者検索のみに絞る
  - ソート順を関連度順（`standard`）に変更
  - 検索結果の並び順を最適化（タイトル検索を優先）
  - デバッグログを追加（検索件数の確認）
  
- ✅ **UserBookDetailViewのデザイン改善**
  - リキッドグラスデザインを適用
  - `.ultraThinMaterial`背景でガラス風のエフェクト
  - 本の画像を白いカードの外に配置（フローティング）
  - 本の角丸を2pxに変更、サイズを一回り小さく調整
  - メモセクションに`.thinMaterial`を適用
  - カード周りに余白を追加（horizontal: 20, vertical: 20）
  
- ✅ **本棚のサムネイル表示問題を修正**
  - BookshelfViewのAsyncImageに`.id(imageURL)`を追加
  - 本を登録後、即座にサムネイルが表示されるように改善
  - アプリを閉じなくても画像が反映される

### 2026-01-21（DAY63）
- ✅ **集計ページ（StatisticsView）の完全実装**
  - Swift Chartsで年別グラフ機能を実装
  - 金額の折れ線グラフ（緑色・直線・小さいポイント）
  - 冊数の棒グラフ（緑色半透明・細い棒・角丸2pt）
  - Y軸を右側に配置、X軸を日本語表記（1月、2月...）
  - グリッドラインは水平方向のみ表示
  
- ✅ **年別スワイプ機能**
  - TabViewでページング実装
  - 左右スワイプで前年/翌年に切り替え
  - ページインジケーター（ドット）でナビゲーション
  - 年表示は固定、グラフ部分のみTabView化
  
- ✅ **データ表示ロジック**
  - 未来の月を表示しない（現在の年は今月まで）
  - 過去の年は1-12月すべて表示
  - データがない月は0として表示
  - X軸は常に12ヶ月分のラベルを表示
  
- ✅ **読書レポートへの入口追加**
  - グラフの下に「あなたの読書レポートを見る→」カード
  - NavigationLinkで別ページに遷移
  - ReadingReportView（プレースホルダー）を作成
  
- ✅ **デバッグ機能の強化**
  - デバッグビルド時にテストデータを自動生成
  - 過去3年分のランダムなデータを作成
  - 3つのテスト口座（技術書、漫画、小説）
  - 各月1-3冊、価格800-8,000円のランダムデータ
  
- ✅ **UIの細かい調整**
  - グラフとラベルの間隔調整
  - フォントサイズ最適化（軸ラベル9pt）
  - ページインジケーターの配置調整
  - 年数表示のカンマ削除

### 2026-01-20（DAY62）
- ✅ **検索画面のUX改善**
  - 検索画面をモーダル表示に戻して`.searchable`の自動配置を活用
  - `.searchable(isPresented:)`パラメータで検索バーに自動フォーカス
  - 登録ボタンを押すと即座にキーボードが表示される体験を実現
  - キャンセルボタンを追加してモーダルを閉じられるように改善
  
- ✅ **MainTabViewの改善**
  - `.navigationDestination`から`.sheet`に変更
  - タブごとの重複を避けるためZStackレベルで`.sheet`を管理
  - より自然なモーダル表示の実装

### 2026-01-19（DAY61）
- ✅ **口座管理機能の完全実装**
  - カスタム口座の作成・削除機能
  - 総合口座 = すべてのカスタム口座の合計表示（仮想口座）
  - UserBookは必ずどこかのカスタム口座に所属
  
- ✅ **タブバー構成の大幅変更**
  - 3タブ（ホーム・口座・本棚）→ 2タブ（通帳・本棚）に変更
  - 口座切り替えボタンを左上に配置（口座名▼）
  - 総合口座の復活（デフォルト表示）
  - 各口座で通帳・本棚を切り替え可能
  
- ✅ **オンボーディング画面（OnboardingView）**
  - 初回起動時に最初の口座開設を促す
  - おすすめ口座名の候補表示（プライベート、漫画、仕事用）
  - FlowLayoutでレスポンシブなボタン配置
  - キーボードに「完了」ボタン表示（submitLabel）
  - 入力欄の右に「口座」ラベル表示
  
- ✅ **口座選択画面（PassbookSelectorView）**
  - フルスクリーンページとして実装
  - 総合口座とカスタム口座をセクション分け
  - NavigationLinkで口座追加画面に遷移（モーダルが重ならない）
  - チェックマークで選択中の口座を表示
  
- ✅ **口座追加画面（AddPassbookView）**
  - キャンセルボタン削除（戻るボタンと重複）
  - おすすめボタンを横スクロールから折り返し表示に変更
  - キーボードに「完了」ボタン表示
  - 入力欄の右に「口座」ラベル表示
  
- ✅ **本登録時の口座選択機能**
  - BookSearchViewに口座選択プルダウン追加
  - AddBookViewに口座選択プルダウン追加
  - 現在開いている口座がデフォルト値として設定
  - どの画面からでも好きな口座に登録可能
  
- ✅ **PassbookDetailViewの総合口座対応**
  - `passbook: Passbook?` で総合口座（nil）とカスタム口座の両方に対応
  - `isOverall: Bool` フラグで表示を切り替え
  - 総合口座: 全カスタム口座の書籍数・金額を集計表示
  - カスタム口座: その口座の書籍のみ表示
  - ナビゲーションタイトルに口座名を表示
  
- ✅ **BookshelfViewの口座対応**
  - 選択中の口座の本のみを表示
  - 総合口座用の本棚ビュー（OverallBookshelfView）を作成
  
- ✅ **重複登録防止機能**
  - 全口座を対象に重複チェック
  - 同じ本を複数の口座に登録できないように変更
  - ISBNまたはタイトル+著者で判定
  
- ✅ **BookBankApp.swiftの初期化ロジック変更**
  - 総合口座の自動作成を廃止
  - カスタム口座がない場合はオンボーディングを表示
  - RootViewで起動時の分岐処理

### 2026-01-18（DAY60）
- ✅ **タブバーのUI改善**
  - カスタムタブバーから標準TabViewに変更
  - 独立した登録ボタン（右下の青い丸ボタン）を配置
  - ホームと本棚は標準タブバーで管理
  
- ✅ **通帳ページの大幅改善**
  - NavigationBarのスタイルを統一（本棚ページと同じに）
  - HomeViewから口座スワイプ機能を削除（シンプル化）
  - 総合口座のみを表示する構造に変更
  - `.listStyle(.insetGrouped)`でiOS標準のグループ化リストスタイルに変更
  - 通帳ページから削除機能を削除（口座スワイプとの競合回避）
  - ScrollViewの挙動修正（タブバーの下まで表示）
  
- ✅ **本棚ページのUI改善**
  - ギャップを2pxに変更（iOS写真アプリスタイル）
  - 角丸を2pxに追加
  - 本のサイズを画面幅の25%に変更（レスポンシブ対応）
  
- ✅ **コードのクリーンアップ**
  - 余分なpadding、ignoresSafeAreaなどを削除
  - iOS標準のListコンポーネントを活用
  - NavigationStackの二重化問題を解消

### 2026-01-17（DAY59）
- ✅ **本の詳細画面完成**
  - UserBookDetailView.swiftの実装
  - 表紙画像（ドロップシャドウ、角丸なし、200px）
  - お気に入りボタン（画像右上）
  - 削除ボタン（価格の右、控えめデザイン）
  - 詳細情報表示（登録日、出版社、出版年、発行形態、ページ数）
- ✅ **メモ編集モーダル実装**
  - MemoEditorView.swiftの作成
  - 全画面モーダル形式
  - キャンセル・完了ボタン
  - 変更確認アラート
  - 即座に保存（ラグなし）
- ✅ **UserBookモデル拡張**
  - imageURL（大サイズ画像のみ保存で効率化）
  - bookFormat（発行形態）
  - pageCount（ページ数）
- ✅ **楽天Books API拡張**
  - 発行形態（size）の取得
  - ページ数の抽出（itemCaptionから正規表現で）
  - 画像は大サイズ優先、なければ中サイズ
- ✅ **UX改善**
  - 通帳画面でスワイプ削除時の確認ダイアログを削除（即削除）
  - 日付フォーマット統一（YYYY.MM.DD）
  - ミニマルなデザイン（アイコン削減、シリーズ削除）

### 2026-01-15（DAY58）
- ✅ **楽天Books API連携完了**
  - RakutenBooksService, RakutenBooksModelsの実装
  - タイトル・著者名での検索（並行実行）
  - 発売日順（新しい順）ソート
  - 無限スクロール（ページング）実装
- ✅ **UX改善**
  - 連続登録機能（検索画面を閉じない）
  - 入金トースト通知（¥XXX 入金しました！）
  - 登録済み本のグレーアウト + 「登録済み」バッジ
  - 「未登録のみを表示」フィルター機能
- ✅ **通帳画面の改善**
  - スワイプで削除機能（確認ダイアログ付き）
  - 銀行通帳風2カラムレイアウト
  - 日付フォーマット変更（YYYY.MM.DD）
  - コンパクトなデザイン

### 2026-01-15（DAY57）
- ✅ 本の検索画面（BookSearchView）作成
- ✅ 手動登録画面（AddBookView）作成 - タイトル・金額必須
- ✅ 通帳画面の+ボタンを検索画面に変更
- ✅ 登録後に通帳画面に戻るUX改善
- ✅ Git構成の修正（サブモジュール→通常のフォルダ管理）
- ✅ 消失ファイルの再作成（BookBankApp.swift, ContentView.swift, Models/など）

### 2026-01-14（DAY55-56）
- ✅ SwiftData ModelContainer設定
- ✅ 総合口座の自動作成機能
- ✅ 口座一覧画面（ContentView）
- ✅ 通帳画面（PassbookDetailView）

### 2026-01-11（DAY54）
- ✅ Xcodeプロジェクト作成
- ✅ SwiftDataモデル定義（Passbook, UserBook, Subscription）
